const SX40_TABLE={24:{standard:9,ull:9},25:{standard:9,ull:9},30:{standard:9,ull:9},48:{standard:9,ull:5.625},50:{standard:9,ull:5.4},60:{standard:9,ull:4.5},72:{standard:7.5,ull:3.75},100:{standard:5.4,ull:2.7},120:{standard:4.5,ull:2.25},144:{standard:3.75,ull:1.875},150:{standard:3.6,ull:1.8},180:{standard:3,ull:1.5},192:{standard:2.8125,ull:1.40625},200:{standard:2.7,ull:1.35},240:{standard:2.25,ull:1.125},250:{standard:2.16,ull:1.08}},S8_TABLE={24:{standard:{8:4.5,10:4.5,12:4.5},ull:{8:4.5,10:4.2,12:3.5}},25:{standard:{8:4.5,10:4.5,12:4.5},ull:{8:4.5,10:4.032,12:3.36}},30:{standard:{8:4.5,10:4.5,12:4.5},ull:{8:4.2,10:3.36,12:2.8}},48:{standard:{8:4.5,10:4.2,12:3.5},ull:{8:2.625,10:2.1,12:1.75}},50:{standard:{8:4.5,10:4.032,12:3.36},ull:{8:2.52,10:2.016,12:1.68}},60:{standard:{8:4.2,10:3.36,12:2.8},ull:{8:2.1,10:1.68,12:1.4}},72:{standard:{8:3.5,10:2.8,12:2.333328},ull:{8:1.75,10:1.4,12:1.166664}},100:{standard:{8:2.52,10:2.016,12:1.68},ull:{8:1.26,10:1.008,12:.84}},120:{standard:{8:2.1,10:1.68,12:1.4},ull:{8:1.05,10:.84,12:.7}},144:{standard:{8:1.75,10:1.4,12:1.166664},ull:{8:.875,10:.7,12:.583328}},150:{standard:{8:1.68,10:1.344,12:1.12},ull:{8:.84,10:.672,12:.56}},180:{standard:{8:1.4,10:1.12,12:.933328},ull:{8:.7,10:.56,12:.466664}},192:{standard:{8:1.312496,10:1.05,12:.875},ull:{8:.656248,10:.525,12:.437496}},200:{standard:{8:1.26,10:1.008,12:.84},ull:{8:.63,10:.504,12:.42}},240:{standard:{8:1.05,10:.84,12:.7},ull:{8:.525,10:.42,12:.35}},250:{standard:{8:1.008,10:.8064,12:.672},ull:{8:.504,10:.4032,12:.336}}},SQ200_TABLE={24:{standard:36,ull:18},25:{standard:36,ull:18},30:{standard:36,ull:18},48:{standard:36,ull:18},50:{standard:36,ull:18},60:{standard:36,ull:18},72:{standard:30,ull:15},100:{standard:21.6,ull:10.8},120:{standard:18,ull:9},144:{standard:15,ull:7.5},150:{standard:14.4,ull:7.2},180:{standard:12,ull:6},192:{standard:11.25,ull:5.625},200:{standard:10.8,ull:5.4},240:{standard:9,ull:4.5},250:{standard:8.64,ull:4.32}},FPS_OPTIONS=["23.976","24","25","29.97","30","48","50","59.94","60","72","100","120","144","150","180","192","200","240","250"],PERF_FPS_KEYS=Object.keys(SX40_TABLE).map(k=>Number.parseInt(k,10)),SX40_UPLINK_PORTS=4,XD_DOWNLINK_PORTS=10,XD_MAX_PANELS_PER_PORT=500,MAX_REGIONS_PER_NODE=4,SX40_INPUT_GROUPS=[{maxHz:60,w:4096,h:2160,label:"4K DCI"},{maxHz:100,w:3072,h:1728,label:"3K"},{maxHz:120,w:2800,h:1575,label:"2.8K"},{maxHz:144,w:2560,h:1440,label:"QHD"},{maxHz:250,w:1920,h:1080,label:"Full HD"}];function getSX40MaxInput(fps){const hz=parseFloat(fps)||24;for(const g of SX40_INPUT_GROUPS)if(hz<=g.maxHz)return g;return SX40_INPUT_GROUPS[SX40_INPUT_GROUPS.length-1]}const SQ200_HELIOS_INPUT_GROUPS=[{maxHz:60,w:7680,h:4320,label:"8K"},{maxHz:120,w:4096,h:2160,label:"4K DCI"},{maxHz:200,w:3072,h:1728,label:"3K"},{maxHz:240,w:2800,h:1575,label:"2.8K"},{maxHz:250,w:2560,h:1440,label:"QHD"}];function getSQ200HeliosMaxInput(fps){const hz=parseFloat(fps)||24;for(const g of SQ200_HELIOS_INPUT_GROUPS)if(hz<=g.maxHz)return g;return SQ200_HELIOS_INPUT_GROUPS[SQ200_HELIOS_INPUT_GROUPS.length-1]}function getInputGroup(profile,fps){const reg=PROCESSOR_REGISTRY[profile];return reg?.hasInputGroups?(reg.inputGroupsFn||getSX40MaxInput)(fps):null}const XD_PORT_TABLE={24:{standard:{8:1312500,10:105e4,12:875e3},ull:{8:656250,10:525e3,12:437500}},25:{standard:{8:126e4,10:1008e3,12:84e4},ull:{8:63e4,10:504e3,12:42e4}},30:{standard:{8:105e4,10:84e4,12:7e5},ull:{8:525e3,10:42e4,12:35e4}},48:{standard:{8:656250,10:525e3,12:437500},ull:{8:328125,10:262500,12:218750}},50:{standard:{8:63e4,10:504e3,12:42e4},ull:{8:315e3,10:252e3,12:21e4}},60:{standard:{8:525e3,10:42e4,12:35e4},ull:{8:262500,10:21e4,12:175e3}},72:{standard:{8:437500,10:35e4,12:291666},ull:{8:218750,10:175e3,12:145833}},100:{standard:{8:315e3,10:252e3,12:21e4},ull:{8:157500,10:126e3,12:105e3}},120:{standard:{8:262500,10:21e4,12:175e3},ull:{8:131250,10:105e3,12:87500}},144:{standard:{8:218750,10:175e3,12:145833},ull:{8:109375,10:87500,12:72916}},150:{standard:{8:21e4,10:168e3,12:14e4},ull:{8:105e3,10:84e3,12:7e4}},180:{standard:{8:175e3,10:14e4,12:116666},ull:{8:87500,10:7e4,12:58333}},192:{standard:{8:164062,10:131250,12:109375},ull:{8:82031,10:65625,12:54687}},200:{standard:{8:157500,10:126e3,12:105e3},ull:{8:78750,10:63e3,12:52500}},240:{standard:{8:131250,10:105e3,12:87500},ull:{8:65625,10:52500,12:43750}},250:{standard:{8:126e3,10:100800,12:84e3},ull:{8:63e3,10:50400,12:42e3}}},HELIOS_MAX_MPIX=35,HELIOS_FIBER_PORTS=8,HELIOS_ETH_PORTS_PER_SWITCH=12,HELIOS_SWITCH_PORT_TABLE={24:{8:1275e3,10:1275e3,12:1062500},25:{8:1225e3,10:1225e3,12:102e4},30:{8:102e4,10:102e4,12:85e4},48:{8:635e3,10:635e3,12:531e3},50:{8:61e4,10:61e4,12:51e4},60:{8:51e4,10:51e4,12:425e3},72:{8:425e3,10:425e3,12:354e3},120:{8:24e4,10:24e4,12:2e5},144:{8:195e3,10:195e3,12:16e4},240:{8:1e5,10:1e5,12:9e4}},HELIOS_FIBER_PORT_TABLE={24:{8:8.5,10:8.5,12:8.5},25:{8:8.5,10:8.5,12:8.5},30:{8:8.5,10:8.5,12:8.5},48:{8:6.35,10:6.35,12:5.3125},50:{8:6.1,10:6.1,12:5.1},60:{8:5.1,10:5.1,12:4.25},72:{8:4.25,10:4.25,12:3.54},120:{8:2.4,10:2.4,12:2},144:{8:1.95,10:1.95,12:1.6},240:{8:1,10:1,12:.9}},HELIOS_FPS_KEYS=[24,25,30,48,50,60,72,120,144,240],HELIOS_UNSUPPORTED_FPS=new Set(["100","150","180","192","200","250"]),FPS_HELIOS_FALLBACK={100:"120",150:"144",180:"144",192:"144",200:"240",250:"240"},PROCESSOR_REGISTRY={"Brompton SX40":{portUnitLabel:"XD",portUnitPrefix:"X",portsPerUnit:10,maxUnits:4,hasInputGroups:!0,inputGroupsFn:getSX40MaxInput,nearestFps:nearestKnownFps,capacityLookup(fps,_bpc,latencyMode){const row=SX40_TABLE[fps]||SX40_TABLE[24];return"ULL"===latencyMode?row.ull:row.standard},portLookup(fps,bpc,latencyMode){const row=XD_PORT_TABLE[fps]||XD_PORT_TABLE[24],mode="ULL"===latencyMode?"ull":"standard";return row[mode][bpc]??row[mode][10]??row[mode][8]}},"Brompton S8":{portUnitLabel:"XD",portUnitPrefix:"X",portsPerUnit:10,maxUnits:1/0,hasInputGroups:!1,nearestFps:nearestKnownFps,capacityLookup(fps,bpc,latencyMode){const map=(S8_TABLE[fps]||S8_TABLE[24])["ULL"===latencyMode?"ull":"standard"];return map[bpc]??map[10]??map[8]},portLookup(fps,bpc,latencyMode){const row=XD_PORT_TABLE[fps]||XD_PORT_TABLE[24],mode="ULL"===latencyMode?"ull":"standard";return row[mode][bpc]??row[mode][10]??row[mode][8]}},"Megapixel HELIOS":{portUnitLabel:"RP12",portUnitPrefix:"RP",portsPerUnit:12,maxUnits:8,hasInputGroups:!0,inputGroupsFn:getSQ200HeliosMaxInput,hardCapMPix:35,infoNote:"Supports up to 8K at ≤60 Hz. FPS 100/150/180/192/200/250 not supported.",nearestFps:nearestHeliosFps,capacityLookup(fps,bpc){const row=HELIOS_FIBER_PORT_TABLE[fps]||HELIOS_FIBER_PORT_TABLE[24],fiberCap=row[bpc]??row[10]??row[8];return Math.min(35,8*fiberCap)},portLookup(fps,bpc){const row=HELIOS_SWITCH_PORT_TABLE[fps]||HELIOS_SWITCH_PORT_TABLE[24];return row[bpc]??row[10]??row[8]}},"Brompton SQ200":{portUnitLabel:"XDS",portUnitPrefix:"S",portsPerUnit:10,maxUnits:24,parentUnit:{label:"QDS",unitsPerParent:12},hasInputGroups:!0,inputGroupsFn:getSQ200HeliosMaxInput,hardCapMPix:36,infoNote:"2 × 100G → 2 QDS → up to 24 XDS → 240 × 1G ports. 8K at ≤60 Hz.",nearestFps:nearestKnownFps,capacityLookup(fps,_bpc,latencyMode){const row=SQ200_TABLE[fps]||SQ200_TABLE[24];return Math.min(36,"ULL"===latencyMode?row.ull:row.standard)},portLookup(fps,bpc,latencyMode){const row=XD_PORT_TABLE[fps]||XD_PORT_TABLE[24],mode="ULL"===latencyMode?"ull":"standard";return row[mode][bpc]??row[mode][10]??row[mode][8]}}},PROCESSOR_PROFILES=[...Object.keys(PROCESSOR_REGISTRY),"Custom"],HISTORY_STORAGE_KEY="led_planner_history_v1",HISTORY_MAX_ENTRIES=30,HISTORY_IGNORED_MESSAGES=new Set(["Pan start","Move start","Resize start","Selection changed","Updated details"]),PRESETS_STORAGE_KEY="led_planner_presets_v1",PRESETS_MAX_ENTRIES=30,state={metadata:{projectName:"Stage A Volume",buildTag:"v2.1",frameRate:"24",colorDepth:"10-bit",colorSpace:"Rec.709",eotf:"Gamma 2.4",syncMode:"Genlock",redundancy:"N+1",latencyMode:"Standard",genlockSource:"Evertz 5601MSC",controlNetwork:"10.10.0.0/24",processorProfile:"Brompton SX40",wiringPattern:"linear"},wall:{wallTilesX:24,wallTilesY:8,tilePxW:176,tilePxH:176,tileMmW:500,tileMmH:500,safetyMargin:10,targetUtil:80},nodes:[{id:uid("node"),name:"Render Node 1",maxWidth:7680,maxHeight:4320,maxMPix:14,outputs:2,backupNodeId:""},{id:uid("node"),name:"Render Node 2",maxWidth:7680,maxHeight:4320,maxMPix:14,outputs:2,backupNodeId:""}],processors:[defaultProcessor("PROC-A"),defaultProcessor("PROC-B")],regions:[{id:uid("reg"),name:"Main Left",x:0,y:0,w:12,h:8,nodeId:"",processorId:"",role:"Frustum"},{id:uid("reg"),name:"Main Right",x:12,y:0,w:12,h:8,nodeId:"",processorId:"",role:"Frustum"}],selection:{type:"region",id:""},activeTab:"selection",logLines:[]},view={zoom:1,panX:0,panY:0,showCableOverlay:!1,globalUnitNumbering:!1,dragging:!1,dragMode:"",dragRegionId:"",dragHandle:"",dragStart:null,panStart:null,nodesCollapsed:!1,procsCollapsed:!1},handles=["nw","ne","sw","se"],roles=["Frustum","Background","Ceiling","Floor","Practical","Spare"],palette=["#ff904f","#60d3ff","#66e6a6","#ec9fff","#ffd974","#ff7bb0"],historyStore={loaded:!1,entries:[]},presetsStore={loaded:!1,entries:[]};let nodeSettingsClipboard=null;const els={canvas:document.getElementById("coverageCanvas"),canvasWrap:document.getElementById("canvasWrap"),toggleLeftBtn:document.getElementById("toggleLeftBtn"),toggleRightBtn:document.getElementById("toggleRightBtn"),toggleLogBtn:document.getElementById("toggleLogBtn"),collapseLeftBtn:document.getElementById("collapseLeftBtn"),collapseRightBtn:document.getElementById("collapseRightBtn"),collapseLogBtn:document.getElementById("collapseLogBtn"),outlinerDrawer:document.getElementById("outlinerDrawer"),detailsDrawer:document.getElementById("detailsDrawer"),logDrawer:document.getElementById("logDrawer"),outlinerList:document.getElementById("outlinerList"),wallQuickStats:document.getElementById("wallQuickStats"),detailsHost:document.getElementById("detailsHost"),plannerStats:document.getElementById("plannerStats"),plannerInfoBtn:document.getElementById("plannerInfoBtn"),toggleCablePlanBtn:document.getElementById("toggleCablePlanBtn"),selectionBadge:document.getElementById("selectionBadge"),canvasStatus:document.getElementById("canvasStatus"),outputLog:document.getElementById("outputLog"),issues:document.getElementById("issues"),importInput:document.getElementById("importInput"),historyBtn:document.getElementById("historyBtn"),historyBadgeCount:document.getElementById("historyBadgeCount"),presetsBtn:document.getElementById("presetsBtn"),presetsBadgeCount:document.getElementById("presetsBadgeCount"),quickFrameRate:document.getElementById("quickFrameRate"),quickLatencyMode:document.getElementById("quickLatencyMode"),quickColorDepth:document.getElementById("quickColorDepth"),themeSelect:document.getElementById("themeSelect"),quickWallTilesX:document.getElementById("quickWallTilesX"),quickWallTilesY:document.getElementById("quickWallTilesY"),quickTilePxW:document.getElementById("quickTilePxW"),quickTilePxH:document.getElementById("quickTilePxH"),quickTileMmW:document.getElementById("quickTileMmW"),quickTileMmH:document.getElementById("quickTileMmH"),quickProcessorProfile:document.getElementById("quickProcessorProfile"),wiringPatternSelect:document.getElementById("wiringPatternSelect"),exportCableSvgBtn:document.getElementById("exportCableSvgBtn"),toggleGlobalXdBtn:document.getElementById("toggleGlobalXdBtn")};function wire(){const addNode=()=>{const node={id:uid("node"),name:`Render Node ${state.nodes.length+1}`,maxWidth:7680,maxHeight:4320,maxMPix:14,outputs:2,backupNodeId:""};state.nodes.push(node),select("node",node.id),renderAll(`Added render node ${node.name}`)},addProcessor=()=>{const processor=defaultProcessor(`PROC-${String.fromCharCode(65+state.processors.length)}`,state.metadata.processorProfile||"Brompton SX40");state.processors.push(processor),select("processor",processor.id),renderAll(`Added processor ${processor.name}`)},addRegion=(options={})=>{const forcedNodeId=options.nodeId||"",selectedRegion=getSelectedObject("region"),preferredNodeId=forcedNodeId||("node"===state.selection.type?state.selection.id:selectedRegion?.nodeId||state.nodes[0]?.id||""),preferredProcessorId="processor"===state.selection.type?state.selection.id:selectedRegion?.processorId||state.processors[0]?.id||"";if(preferredNodeId){const node=state.nodes.find(n=>n.id===preferredNodeId),maxRegions=node?.outputs||4;if(state.regions.filter(r=>r.nodeId===preferredNodeId).length>=maxRegions)return renderLog(`Cannot add region: ${node?.name||"node"} already has ${maxRegions} output(s) (max).`),void showToast(`${node?.name||"Node"} is at the ${maxRegions}-output limit`)}const smartPlacement=suggestSmartPlacementForProcessor(preferredProcessorId),region={id:uid("reg"),name:`Region ${state.regions.length+1}`,x:smartPlacement.x,y:smartPlacement.y,w:smartPlacement.w,h:smartPlacement.h,nodeId:preferredNodeId,processorId:preferredProcessorId,role:"Background"};state.regions.push(region),select("region",region.id),renderAll(`Added render area ${region.name} (smart-sized)`)};document.getElementById("addNodeBtn")?.addEventListener("click",addNode),document.getElementById("addProcessorBtn")?.addEventListener("click",addProcessor),document.getElementById("addRegionBtn")?.addEventListener("click",addRegion),document.getElementById("howToBtn")?.addEventListener("click",showHowTo),document.getElementById("howToBtn")?.addEventListener("keydown",e=>{"Enter"!==e.key&&" "!==e.key||(e.preventDefault(),showHowTo())}),els.historyBtn?.addEventListener("click",showHistoryModal),els.historyBtn?.addEventListener("keydown",e=>{"Enter"!==e.key&&" "!==e.key||(e.preventDefault(),showHistoryModal())}),els.presetsBtn?.addEventListener("click",showPresetsModal),els.presetsBtn?.addEventListener("keydown",e=>{"Enter"!==e.key&&" "!==e.key||(e.preventDefault(),showPresetsModal())}),document.getElementById("removeSelectedBtn").addEventListener("click",removeSelected),document.getElementById("fitViewBtn").addEventListener("click",()=>{fitView(),renderAll("Canvas fit to screen")}),document.getElementById("validateBtn").addEventListener("click",()=>renderAll("Validation run")),document.getElementById("suggestConfigsBtn")?.addEventListener("click",suggestConfigs),document.getElementById("suggestConfigsBtn")?.addEventListener("keydown",e=>{"Enter"!==e.key&&" "!==e.key||(e.preventDefault(),suggestConfigs())}),document.getElementById("autoPackBtn")?.addEventListener("click",autoPackRegionsForCurrentSettings),document.getElementById("processorOverviewBtn")?.addEventListener("click",showProcessorOverview),els.toggleCablePlanBtn?.addEventListener("click",toggleCablePlanOverlay),els.wiringPatternSelect?.addEventListener("change",()=>{state.metadata.wiringPattern=els.wiringPatternSelect.value,renderAll(`Wiring pattern: ${els.wiringPatternSelect.value}`)}),els.toggleGlobalXdBtn?.addEventListener("click",()=>{view.globalUnitNumbering=!view.globalUnitNumbering,syncCanvasMetaButtons(),renderCanvas(calculateReport()),renderLog("Global unit numbering "+(view.globalUnitNumbering?"enabled":"disabled"))}),document.getElementById("exportBtn").addEventListener("click",exportConfig),document.getElementById("exportSvgBtn")?.addEventListener("click",exportSvg),els.exportCableSvgBtn?.addEventListener("click",exportCableSvg),document.getElementById("clearLogBtn").addEventListener("click",()=>{state.logLines=[],renderLog("Log cleared")}),els.quickFrameRate?.addEventListener("change",()=>{state.metadata.frameRate=els.quickFrameRate.value,showToast(`Frame rate updated to ${state.metadata.frameRate} fps`),renderAll(`Frame rate set to ${state.metadata.frameRate} fps`)}),els.quickLatencyMode?.addEventListener("change",()=>{state.metadata.latencyMode=els.quickLatencyMode.value,showToast(`Latency mode updated to ${state.metadata.latencyMode}`),renderAll(`Latency mode set to ${state.metadata.latencyMode}`)}),els.quickColorDepth?.addEventListener("change",()=>{state.metadata.colorDepth=els.quickColorDepth.value,showToast(`Color depth updated to ${state.metadata.colorDepth}`),renderAll(`Color depth set to ${state.metadata.colorDepth}`)}),els.themeSelect?.addEventListener("change",()=>{applyTheme(els.themeSelect.value),showToast(`Theme set to ${els.themeSelect.value}`),renderAll(`Theme set to ${els.themeSelect.value}`)});[{el:"quickWallTilesX",key:"wallTilesX"},{el:"quickWallTilesY",key:"wallTilesY"},{el:"quickTilePxW",key:"tilePxW"},{el:"quickTilePxH",key:"tilePxH"},{el:"quickTileMmW",key:"tileMmW"},{el:"quickTileMmH",key:"tileMmH"}].forEach(({el:el,key:key})=>{const input=els[el];input&&input.addEventListener("change",()=>{const v=parseInt(input.value,10);if(Number.isFinite(v)&&v>0){state.wall[key]=v,sanitizeWall(),input.value=state.wall[key];showToast(`${{wallTilesX:"Tiles X",wallTilesY:"Tiles Y",tilePxW:"Cabinet W (px)",tilePxH:"Cabinet H (px)",tileMmW:"Cabinet W (mm)",tileMmH:"Cabinet H (mm)"}[key]||key} updated to ${state.wall[key]}`),renderAll(`Wall ${key} set to ${state.wall[key]}`)}})}),els.quickProcessorProfile?.addEventListener("change",()=>{state.metadata.processorProfile=els.quickProcessorProfile.value,syncProcessorProfiles();let fpsCorrectionNote="";if("Megapixel HELIOS"===state.metadata.processorProfile&&HELIOS_UNSUPPORTED_FPS.has(state.metadata.frameRate)){const corrected=FPS_HELIOS_FALLBACK[state.metadata.frameRate]||"60";state.metadata.frameRate=corrected,els.quickFrameRate&&(els.quickFrameRate.value=corrected),fpsCorrectionNote=` (FPS auto-corrected to ${corrected})`}showToast(`Processor type set to ${state.metadata.processorProfile}${fpsCorrectionNote}`),renderAll(`Processor type set to ${state.metadata.processorProfile}${fpsCorrectionNote}`)}),els.toggleLeftBtn?.addEventListener("click",()=>toggleDrawer(els.outlinerDrawer)),els.toggleRightBtn?.addEventListener("click",()=>toggleDrawer(els.detailsDrawer)),els.toggleLogBtn?.addEventListener("click",()=>toggleDrawer(els.logDrawer)),els.collapseLeftBtn?.addEventListener("click",()=>{setDrawerOpen(els.outlinerDrawer,!1),syncDrawerButtons()}),els.collapseRightBtn?.addEventListener("click",()=>{setDrawerOpen(els.detailsDrawer,!1),syncDrawerButtons()}),els.plannerInfoBtn?.addEventListener("click",()=>{els.plannerStats?.classList.toggle("is-open")}),els.collapseLogBtn?.addEventListener("click",()=>{setDrawerOpen(els.logDrawer,!1),syncDrawerButtons()}),els.importInput.addEventListener("change",importConfig),document.querySelectorAll(".tab-btn").forEach(btn=>{btn.addEventListener("click",()=>{state.activeTab=btn.dataset.tab,document.querySelectorAll(".tab-btn").forEach(b=>b.classList.toggle("active",b===btn)),setDrawerOpen(els.detailsDrawer,!0),syncDrawerButtons(),renderDetails(calculateReport())})}),window.addEventListener("resize",()=>{resizeCanvas(),renderCanvas(calculateReport())}),els.canvas.addEventListener("pointerdown",onPointerDown),els.canvas.addEventListener("contextmenu",e=>e.preventDefault()),window.addEventListener("pointermove",onPointerMove),window.addEventListener("pointerup",onPointerUp),els.canvas.addEventListener("wheel",onWheel,{passive:!1}),setupToolMenus(),setDrawerOpen(els.outlinerDrawer,!1),setDrawerOpen(els.detailsDrawer,!1),setDrawerOpen(els.logDrawer,!1),syncDrawerButtons(),loadHistoryStore(),loadPresetsStore(),state._addNode=addNode,state._addProcessor=addProcessor,state._addRegion=addRegion}function setupToolMenus(){const menus=Array.from(document.querySelectorAll(".tool-menu"));menus.forEach(menu=>{menu.addEventListener("toggle",()=>{menu.open&&(menus.forEach(other=>{other!==menu&&(other.open=!1)}),positionToolMenu(menu))}),menu.addEventListener("click",event=>{event.target.closest("button, .menu-file-btn")&&menu.open&&(menu.open=!1)})}),document.addEventListener("pointerdown",event=>{menus.forEach(menu=>{menu.open&&!menu.contains(event.target)&&(menu.open=!1)})}),document.addEventListener("keydown",event=>{if("Escape"===event.key)return menus.forEach(menu=>{menu.open=!1}),void els.plannerStats?.classList.remove("is-open");if("Enter"===event.key){const active=document.activeElement;return void(active&&["INPUT","SELECT","TEXTAREA"].includes(active.tagName)||(setDrawerOpen(els.detailsDrawer,!1),syncDrawerButtons()))}if("Delete"===event.key||"Backspace"===event.key){const active=document.activeElement;if(active&&["INPUT","SELECT","TEXTAREA"].includes(active.tagName))return;"region"===state.selection.type&&removeItemById("region",state.selection.id)}}),window.addEventListener("resize",()=>{menus.forEach(menu=>{menu.open&&positionToolMenu(menu)})}),document.addEventListener("pointerdown",event=>{const stats=els.plannerStats,infoBtn=els.plannerInfoBtn;stats?.classList.contains("is-open")&&(stats.contains(event.target)||infoBtn?.contains(event.target)||stats.classList.remove("is-open"))})}function positionToolMenu(menu){const panel=menu.querySelector(".tool-menu-panel");if(!panel)return;panel.style.transform="translateX(0)";const rect=panel.getBoundingClientRect();let shiftX=0;rect.right>window.innerWidth-8&&(shiftX-=rect.right-(window.innerWidth-8)),rect.left<8&&(shiftX+=8-rect.left),panel.style.transform=0===shiftX?"translateX(0)":`translateX(${Math.round(shiftX)}px)`}function bootstrap(){state.regions[0].nodeId=state.nodes[0].id,state.regions[1].nodeId=state.nodes[1].id,state.regions[0].processorId=state.processors[0].id,state.regions[1].processorId=state.processors[1].id,state.selection={type:"region",id:state.regions[0].id},resizeCanvas()}function defaultProcessor(name,profile="Brompton SX40"){return{id:uid("proc"),name:name,type:profile,profile:profile,capacityMode:"profile",latencyMode:"From Project",customMPix:9}}function syncProcessorProfiles(){const profile=state.metadata.processorProfile||"Brompton SX40";state.processors.forEach(p=>{p.profile=profile})}function resizeCanvas(){const w=Math.max(300,Math.floor(els.canvasWrap.clientWidth)),h=Math.max(220,Math.floor(els.canvasWrap.clientHeight));els.canvas.width===w&&els.canvas.height===h||(els.canvas.width=w,els.canvas.height=h)}function fitView(){resizeCanvas();const zw=(els.canvas.width-112)/state.wall.wallTilesX,zh=(els.canvas.height-112)/state.wall.wallTilesY;view.zoom=clampFloat(Math.min(zw,zh),7,140),view.panX=(els.canvas.width-state.wall.wallTilesX*view.zoom)/2,view.panY=(els.canvas.height-state.wall.wallTilesY*view.zoom)/2}function renderAll(message,options={}){const{skipDetails:skipDetails=!1}=options;syncQuickControls();const report=calculateReport();renderOutliner(report),renderQuickStats(report),renderPlannerStats(report),skipDetails||renderDetails(report),renderCanvas(report),renderIssues(report.issues),renderSelectionBadge(),syncCanvasMetaButtons(),message&&(renderLog(message),pushHistoryEntry(message))}function syncCanvasMetaButtons(){els.toggleCablePlanBtn&&(els.toggleCablePlanBtn.classList.toggle("primary",!!view.showCableOverlay),els.toggleCablePlanBtn.title=view.showCableOverlay?"Disable cable plan overlay":"Enable cable plan overlay",els.toggleGlobalXdBtn&&(els.toggleGlobalXdBtn.classList.toggle("primary",!!view.globalUnitNumbering),els.toggleGlobalXdBtn.title=view.globalUnitNumbering?"Disable global unit numbering (back to per-processor numbering)":"Enable global unit numbering - units numbered sequentially across all processors"))}function updateFpsOptionsForProcessor(profile){if(!els.quickFrameRate)return;const isHelios="Megapixel HELIOS"===profile;for(const opt of els.quickFrameRate.options)opt.disabled=isHelios&&HELIOS_UNSUPPORTED_FPS.has(opt.value)}function syncQuickControls(){if(els.quickFrameRate&&(els.quickFrameRate.value=state.metadata.frameRate),els.quickLatencyMode&&(els.quickLatencyMode.value=state.metadata.latencyMode),els.quickColorDepth&&(els.quickColorDepth.value=state.metadata.colorDepth),els.themeSelect){const theme=document.body.dataset.theme||"dark";els.themeSelect.value=theme}els.quickProcessorProfile&&(els.quickProcessorProfile.value=state.metadata.processorProfile||"Brompton SX40"),els.wiringPatternSelect&&(els.wiringPatternSelect.value=state.metadata.wiringPattern||"linear"),updateFpsOptionsForProcessor(state.metadata.processorProfile||"Brompton SX40"),els.quickWallTilesX&&(els.quickWallTilesX.value=state.wall.wallTilesX),els.quickWallTilesY&&(els.quickWallTilesY.value=state.wall.wallTilesY),els.quickTilePxW&&(els.quickTilePxW.value=state.wall.tilePxW),els.quickTilePxH&&(els.quickTilePxH.value=state.wall.tilePxH),els.quickTileMmW&&(els.quickTileMmW.value=state.wall.tileMmW),els.quickTileMmH&&(els.quickTileMmH.value=state.wall.tileMmH)}function initTheme(){let theme="dark";try{const saved=localStorage.getItem("led_planner_theme");saved&&(theme=saved)}catch{}applyTheme(theme)}function applyTheme(theme){const resolved=new Set(["dark","light","blue","lime"]).has(theme)?theme:"dark";document.body.dataset.theme=resolved,els.themeSelect&&(els.themeSelect.value=resolved);try{localStorage.setItem("led_planner_theme",resolved)}catch{}}function toggleDrawer(drawer){drawer&&(drawer.classList.toggle("is-open"),syncDrawerButtons())}function setDrawerOpen(drawer,open){drawer&&drawer.classList.toggle("is-open",!!open)}function syncDrawerButtons(){els.toggleLeftBtn&&els.toggleLeftBtn.classList.toggle("primary",els.outlinerDrawer?.classList.contains("is-open")),els.toggleRightBtn&&els.toggleRightBtn.classList.toggle("primary",els.detailsDrawer?.classList.contains("is-open")),els.toggleLogBtn&&els.toggleLogBtn.classList.toggle("primary",els.logDrawer?.classList.contains("is-open"))}function renderOutliner(report){const html=[];html.push(`<div class="outliner-section-head">\n    <div class="meta-pill">Render Nodes (${state.nodes.length})</div>\n    <div class="outliner-section-actions">\n      <button class="tool-btn toolbar-add tiny" data-action="add-node" title="Add render node">+ Node</button>\n      <button class="tool-btn icon-btn tiny collapse-btn" data-action="toggle-nodes-section" title="${view.nodesCollapsed?"Expand":"Collapse"} all nodes">${view.nodesCollapsed?"▶":"▼"}</button>\n    </div>\n  </div>`),view.nodesCollapsed||state.nodes.forEach((node,nodeIndex)=>{const usedMPix=(report.nodeLoads[node.id]||{totalPixels:0,regions:[]}).totalPixels/1e6,nodeMaxMPix=node.maxWidth*node.maxHeight/1e6,percent=nodeMaxMPix>0?usedMPix/nodeMaxMPix*100:0,nodeNum=nodeIndex+1,owned=state.regions.filter(r=>r.nodeId===node.id);html.push(`<div class="node-block node-selectable ${isSelected("node",node.id)?"active-cell":""}" data-node-cell-id="${node.id}">\n        <div class="node-head">\n          <div class="node-title" title="Select node to edit render limits">\n            <span class="color-dot" style="background:${colorForNode(node.id)};"></span>\n            <span class="node-num">#${nodeNum}</span>\n            <span class="icon-mask outliner-icon icon-node"></span> ${escapeHtml(node.name)}\n          </div>\n          <div class="node-head-actions">\n            <span class="node-mpix-pill">${percent.toFixed(0)}% &bull; ${usedMPix.toFixed(2)}/${nodeMaxMPix.toFixed(2)} MPix</span>\n            <button class="tool-btn icon-btn tiny delete-btn" data-action="delete-node" data-node-id="${node.id}" title="Delete node">✕</button>\n          </div>\n        </div>\n        <div class="region-list">`),0===owned.length?html.push('<span class="meta-pill muted-pill">No assigned regions</span>'):owned.forEach(region=>{const regionIndex=Math.max(0,state.regions.findIndex(r=>r.id===region.id));html.push(`<div class="region-row"><button class="region-btn ${isSelected("region",region.id)?"active":""}" data-select-type="region" data-select-id="${region.id}" title="Select render area"><span class="color-dot" style="background:${colorForRegion(region,regionIndex)};"></span><span class=\\"icon-mask outliner-icon icon-region\\"></span> ${escapeHtml(region.name)} (${region.w}x${region.h})</button><button class="tool-btn icon-btn tiny delete-btn" data-action="delete-region" data-region-id="${region.id}" title="Delete region">✕</button></div>`)}),html.push(`</div><button class="tool-btn toolbar-add tiny node-add-region" data-action="add-region-for-node" data-node-id="${node.id}" title="Add a render region assigned to ${escapeAttr(node.name)}">+ Add Region</button></div>`)}),html.push(`<div class="outliner-section-head">\n    <div class="meta-pill">Processors (${state.processors.length})</div>\n    <div class="outliner-section-actions">\n      <button class="tool-btn toolbar-add tiny" data-action="add-processor" title="Add processor">+ Processor</button>\n      <button class="tool-btn icon-btn tiny collapse-btn" data-action="toggle-procs-section" title="${view.procsCollapsed?"Expand":"Collapse"} all processors">${view.procsCollapsed?"▶":"▼"}</button>\n    </div>\n  </div>`),view.procsCollapsed||state.processors.forEach(processor=>{const load=report.processorLoads[processor.id]||{totalPixels:0,capacityMPix:0,xdPortsRequired:0,xdsRequired:0},used=load.totalPixels/1e6,cap=load.capacityMPix||0,percent=cap>0?used/cap*100:0;html.push(`<div class="node-block node-selectable ${isSelected("processor",processor.id)?"active-cell":""}" data-processor-cell-id="${processor.id}">\n        <div class="node-head">\n          <div class="node-title" title="Select processor to edit throughput profile"><span class="icon-mask outliner-icon icon-proc"></span> ${escapeHtml(processor.name)}</div>\n          <div class="node-head-actions">\n            <span class="node-mpix-pill">${percent.toFixed(0)}% &bull; ${used.toFixed(2)}/${cap.toFixed(2)} MPix</span>\n            <button class="tool-btn icon-btn tiny delete-btn" data-action="delete-processor" data-processor-id="${processor.id}" title="Delete processor">✕</button>\n          </div>\n        </div>\n        <div class="proc-meta">\n          <div class="proc-meta-row"><span class="proc-k">Model</span><span class="proc-v">${escapeHtml(processorModeLabel(processor))}</span></div>\n          <div class="proc-meta-row"><span class="proc-k">${load.portUnitLabel||"XD"}</span><span class="proc-v">${load.xdPortsRequired} ports / ${load.xdsRequired} ${load.portUnitLabel||"XD"}(s)</span></div>\n        </div>\n      </div>`)});const unassigned=state.regions.filter(r=>!r.nodeId);html.push('<div class="outliner-section-head"><div class="meta-pill">Unassigned Regions</div></div>'),unassigned.length&&(html.push('<div class="node-block"><div class="region-list">'),unassigned.forEach(region=>{const regionIndex=Math.max(0,state.regions.findIndex(r=>r.id===region.id));html.push(`<div class="region-row"><button class="region-btn ${isSelected("region",region.id)?"active":""}" data-select-type="region" data-select-id="${region.id}" title="Select render area"><span class="color-dot" style="background:${colorForRegion(region,regionIndex)};"></span><span class=\\"icon-mask outliner-icon icon-region\\"></span> ${escapeHtml(region.name)}</button><button class="tool-btn icon-btn tiny delete-btn" data-action="delete-region" data-region-id="${region.id}" title="Delete region">✕</button></div>`)}),html.push("</div></div>")),els.outlinerList.innerHTML=html.join(""),els.outlinerList.querySelectorAll("[data-select-id]").forEach(el=>{el.addEventListener("click",()=>{select(el.dataset.selectType,el.dataset.selectId),"node"!==el.dataset.selectType&&"processor"!==el.dataset.selectType||(setDrawerOpen(els.detailsDrawer,!0),syncDrawerButtons()),renderAll("Selection changed")})}),els.outlinerList.querySelectorAll("[data-node-cell-id]").forEach(cell=>{cell.addEventListener("click",event=>{event.target.closest(".region-btn")||event.target.closest(".node-add-region")||event.target.closest(".delete-btn")||(select("node",cell.dataset.nodeCellId),setDrawerOpen(els.detailsDrawer,!0),syncDrawerButtons(),renderAll("Selection changed"))})}),els.outlinerList.querySelectorAll("[data-processor-cell-id]").forEach(cell=>{cell.addEventListener("click",event=>{event.target.closest(".region-btn")||event.target.closest(".node-add-region")||event.target.closest(".delete-btn")||(select("processor",cell.dataset.processorCellId),setDrawerOpen(els.detailsDrawer,!0),syncDrawerButtons(),renderAll("Selection changed"))})}),els.outlinerList.querySelectorAll("[data-action]").forEach(el=>{el.addEventListener("click",event=>{event.stopPropagation();const action=el.dataset.action;"add-node"===action&&state._addNode?.(),"add-processor"===action&&state._addProcessor?.(),"add-region-for-node"===action&&state._addRegion?.({nodeId:el.dataset.nodeId||""}),"delete-node"===action&&removeItemById("node",el.dataset.nodeId||""),"delete-processor"===action&&removeItemById("processor",el.dataset.processorId||""),"delete-region"===action&&removeItemById("region",el.dataset.regionId||""),"toggle-nodes-section"===action&&(view.nodesCollapsed=!view.nodesCollapsed,renderAll()),"toggle-procs-section"===action&&(view.procsCollapsed=!view.procsCollapsed,renderAll())})})}function removeItemById(type,id){id&&(select(type,id),removeSelected())}function renderQuickStats(report){els.wallQuickStats.innerHTML=[`Canvas: <strong>${report.wall.wallPxW}x${report.wall.wallPxH}</strong>`,`Tiles: <strong>${report.wall.wallTilesX}x${report.wall.wallTilesY}</strong>`,`Physical: <strong>${(report.wall.wallMmW/1e3).toFixed(2)}m x ${(report.wall.wallMmH/1e3).toFixed(2)}m</strong>`,`Coverage: <strong>${report.wall.coveragePct.toFixed(1)}%</strong>`].join("<br>"),els.canvasStatus.textContent=`Zoom ${view.zoom.toFixed(1)} px/tile | Pan (${view.panX.toFixed(0)}, ${view.panY.toFixed(0)}) | Cabinet ${state.wall.tilePxW}x${state.wall.tilePxH}px`}function renderPlannerStats(report){if(!els.plannerStats)return;const reference=getReferenceProcessor(),referenceNode=getReferenceNode();if(!reference)return void(els.plannerStats.textContent="No processor configured");if(!referenceNode)return void(els.plannerStats.textContent="No render node configured");const resolved=resolveProcessorCapacity(reference,state.metadata),portData=resolvePortCapacityForProcessor(reference,state.metadata),portUnitLabel=portData.portUnitLabel,procLabel=(reference.profile||"Brompton SX40").split(" ").pop(),tilePixels=Math.max(1,state.wall.tilePxW*state.wall.tilePxH),totalPanels=Math.max(1,report.wall.wallTilesX*report.wall.wallTilesY),processorCapPixels=Math.floor(1e6*resolved.capacityMPix),nodeMpixCapPixels=Math.floor(1e6*referenceNode.maxMPix),nodeDimCapPixels=Math.max(1,referenceNode.maxWidth*referenceNode.maxHeight),effectivePixelsPerProcessor=Math.max(1,Math.min(processorCapPixels,nodeMpixCapPixels,nodeDimCapPixels)),inputGroup=getInputGroup(reference.profile,state.metadata.frameRate),effectiveMaxW=inputGroup?Math.min(referenceNode.maxWidth,inputGroup.w):referenceNode.maxWidth,effectiveMaxH=inputGroup?Math.min(referenceNode.maxHeight,inputGroup.h):referenceNode.maxHeight,processorsByGeometry=Math.max(1,Math.ceil(report.wall.wallPxW/Math.max(1,effectiveMaxW)))*Math.max(1,Math.ceil(report.wall.wallPxH/Math.max(1,effectiveMaxH))),maxTilesWByNode=Math.max(1,Math.floor(effectiveMaxW/Math.max(1,state.wall.tilePxW))),maxTilesHByNode=Math.max(1,Math.floor(effectiveMaxH/Math.max(1,state.wall.tilePxH))),settingsText=`${state.metadata.frameRate} Hz | ${resolved.latencyMode} | ${state.metadata.colorDepth}`;if(tilePixels>portData.portPixels){const errorLines=[`Ref Profile: <strong>${escapeHtml(reference.profile)}</strong>`,`Config: <strong>${escapeHtml(settingsText)}</strong>`,`Canvas: <strong>${report.wall.wallPxW}x${report.wall.wallPxH}</strong>`,`Tiles: <strong>${report.wall.wallTilesX}x${report.wall.wallTilesY}</strong>`,`Processor Capacity: <strong>${resolved.capacityMPix.toFixed(3)} MPix/frame</strong>`,`${portUnitLabel} Port Capacity: <strong style="color:#ff4f63">${formatInt(portData.portPixels)} px/port</strong>`,`Panel Resolution: <strong style="color:#ff4f63">${state.wall.tilePxW}x${state.wall.tilePxH} = ${formatInt(tilePixels)} px</strong>`,'<hr class="hw-divider">','<div class="hw-error"><strong style="color:#ff4f63">Configuration Not Possible</strong><br>',`A single panel (${formatInt(tilePixels)} px) exceeds the ${portUnitLabel} port capacity (${formatInt(portData.portPixels)} px/port) at ${escapeHtml(settingsText)}.<br><br>`,`Each ${portUnitLabel} port must be able to drive at least one full panel. Reduce frame rate, bit depth, or switch to Standard latency.</div>`];return void(els.plannerStats.innerHTML=errorLines.join("<br>"))}const panelCapByNodeGeometry=maxTilesWByNode*maxTilesHByNode,panelCapByPixels=Math.max(1,Math.floor(effectivePixelsPerProcessor/tilePixels)),panelsPerPort=Math.floor(portData.portPixels/tilePixels),portsPerProcessor=Math.max(1,getPortsPerProcessorLimit(reference)),panelCapByPorts=panelsPerPort*portsPerProcessor,panelCeilingPerProcessor=Math.max(1,Math.min(panelCapByNodeGeometry,panelCapByPixels,panelCapByPorts,2e3)),processorsByPanelCapacity=Math.max(1,Math.ceil(totalPanels/panelCeilingPerProcessor)),maxTilesWForStats=maxTilesWByNode,maxTilesHForStats=maxTilesHByNode,bandResult=findOptimalBandLayout(report.wall.wallTilesX,report.wall.wallTilesY,maxTilesWForStats,maxTilesHForStats,panelCeilingPerProcessor,panelsPerPort,processorCapPixels,tilePixels,portData.portsPerUnit),totalProcessorsRequired=bandResult?bandResult.totalProcessors:Math.max(processorsByGeometry,processorsByPanelCapacity),totalXdUnitsRequired=bandResult?bandResult.totalXds:0,totalXdPortsRequired=totalPanels*Math.ceil(tilePixels/Math.max(1,portData.portPixels));let bandDesc="";if(bandResult&&bandResult.bands.length>0){bandDesc=bandResult.bands.map(band=>{const cols=band.regions.length,w=band.regions[0]?.w||0;return`${band.regions.every(r=>r.w===w)?`${cols} x ${w}x${band.h} tiles`:`${cols} regions (mixed widths) x ${band.h}h`}`}).join(" + ")}const inputGroupLabel=inputGroup?`Input Group: <strong>${inputGroup.label} (${inputGroup.w}x${inputGroup.h} @ ≤${inputGroup.maxHz}Hz)</strong>`:"",statsLines=[`Ref Profile: <strong>${escapeHtml(reference.profile)}</strong>`,`Config: <strong>${escapeHtml(settingsText)}</strong>`,inputGroupLabel,`Canvas: <strong>${report.wall.wallPxW}x${report.wall.wallPxH}</strong>`,`Tiles: <strong>${report.wall.wallTilesX}x${report.wall.wallTilesY}</strong>`,`Processor Capacity: <strong>${resolved.capacityMPix.toFixed(3)} MPix/frame</strong>`,`${portUnitLabel} Port Capacity: <strong>${formatInt(portData.portPixels)} px/port</strong>`,`Panels/Port: <strong>${panelsPerPort}</strong> | Ports/Proc: <strong>${portsPerProcessor}</strong>`,`Max Region: <strong>${effectiveMaxW}x${effectiveMaxH} px</strong>`,`True Min (combined): <strong>${totalProcessorsRequired}</strong>`,bandDesc?`Optimal packing: ${bandDesc}`:"",(()=>{const pu=PROCESSOR_REGISTRY[reference.profile]?.parentUnit;if(pu){const numProcs=report.processors?.length||state.processors.length||1,qds=Math.max(numProcs,Math.ceil(totalXdUnitsRequired/pu.unitsPerParent)),xds=Math.max(qds,totalXdUnitsRequired);return`Total ${portUnitLabel} Units: <strong>${xds}</strong> · <strong>${qds} ${pu.label}</strong> (${totalXdPortsRequired} ${portUnitLabel} ports total)`}return`Total ${portUnitLabel} Units: <strong>${totalXdUnitsRequired}</strong> (${totalXdPortsRequired} ${portUnitLabel} ports total)`})()],hw=calculateOptimalHardware();let hwHtml="";if(hw.feasible){const b=hw.best;let splitDesc="",utilDesc="",applyTitle="";if("uniform"===b.type)splitDesc=`Split: <strong>${b.cols}x${b.rows}</strong> (${b.segTilesW}x${b.segTilesH} tiles = ${b.segPxW}x${b.segPxH}px each)<br>Panels/Region: <strong>${b.segPanels}</strong>`,utilDesc=`Utilization: <strong>${b.utilization.toFixed(1)}%</strong>`,applyTitle=`Create ${b.processors} ${procLabel}(s) with uniform ${b.cols}x${b.rows} grid`;else{const bandParts=b.bands.map(band=>{const w=band.regions[0]?.w||0,allSame=band.regions.every(r=>r.w===w),tilePxW=state.wall.tilePxW,tilePxH=state.wall.tilePxH;return allSame?`${band.regions.length} x ${w}x${band.h} (${w*tilePxW}x${band.h*tilePxH}px, ${w*band.h} panels)`:`${band.regions.length} regions x ${band.h}h (mixed widths)`});splitDesc=`Split: <strong>${b.bands.length} band(s)</strong><br>${bandParts.map(p=>`&nbsp;&nbsp;${p}`).join("<br>")}`,utilDesc=`Avg Util: <strong>${b.avgUtil.toFixed(1)}%</strong>`,applyTitle=`Create ${b.processors} ${procLabel}(s) with ${b.bands.length}-band layout`}const altLines=hw.alternatives.map(a=>{const pu=hw.settings.parentUnit;let aXds=a.totalXds,parentStr="";if(pu){const aQds=Math.max(a.processors,Math.ceil(a.totalXds/pu.unitsPerParent));aXds=Math.max(aQds,a.totalXds),parentStr=` / ${aQds} ${pu.label}`}return"uniform"===a.type?`<span class="hw-alt">${a.processors} ${procLabel} (${a.cols}x${a.rows}) ${a.segPxW}x${a.segPxH}px ${a.utilization.toFixed(0)}% util, ${aXds} ${portUnitLabel}${parentStr}</span>`:`<span class="hw-alt">${a.processors} ${procLabel} (${a.bands.length} bands) ${a.avgUtil.toFixed(0)}% avg util, ${aXds} ${portUnitLabel}${parentStr}</span>`}).join("<br>");hwHtml=`\n      <hr class="hw-divider">\n      <div class="hw-recommendation">\n        <strong>Hardware Recommendation</strong><br>\n        ${(()=>{const pu=hw.settings.parentUnit;let bXds=b.totalXds,puStr="";if(pu){const bQds=Math.max(b.processors,Math.ceil(b.totalXds/pu.unitsPerParent));bXds=Math.max(bQds,b.totalXds),puStr=` (<strong>${bQds} ${pu.label}</strong>)`}return`<span class="hw-headline"><strong>${b.processors} ${procLabel}${b.processors>1?"s":""}</strong>, <strong>${bXds} ${portUnitLabel}${bXds>1?"s":""}</strong>${puStr} total</span>`})()}<br>\n        ${splitDesc}<br>\n        ${utilDesc}<br>\n        Panels/Port: <strong>${b.panelsPerPort}</strong> | Max Panels/${procLabel}: <strong>${b.maxPanelsPerSX40}</strong>\n        ${altLines?`<br><span style="color:var(--muted);font-size:0.62rem">Alternatives:<br>${altLines}</span>`:""}\n        <button id="applyHwRecommendation" class="tool-btn primary hw-apply-btn" title="${escapeAttr(applyTitle)}">Apply Recommendation</button>\n      </div>`}else hwHtml=`\n      <hr class="hw-divider">\n      <div class="hw-error">\n        <strong style="color:#ff4f63">No Feasible Config</strong><br>\n        ${escapeHtml(hw.error)}\n      </div>`;els.plannerStats.innerHTML=statsLines.join("<br>")+hwHtml;const applyBtn=document.getElementById("applyHwRecommendation");applyBtn&&applyBtn.addEventListener("click",e=>{e.stopPropagation(),applyHardwareRecommendation(hw)})}function getReferenceProcessor(){if("processor"===state.selection.type){const selected=getSelectedObject("processor");if(selected)return selected}return state.processors[0]||null}function getReferenceNode(){if("node"===state.selection.type){const selected=getSelectedObject("node");if(selected)return selected}return state.nodes[0]||null}function findOptimalBandLayout(wallTilesX,wallTilesY,maxTilesW,maxTilesH,maxPanelsPerProc,panelsPerPort,procCapPixels,tilePixels,portsPerUnit=10){const dp=new Array(wallTilesY+1).fill(1/0),pick=new Array(wallTilesY+1).fill(0);dp[0]=0;for(let remaining=1;remaining<=wallTilesY;remaining++)for(let h=1;h<=Math.min(remaining,maxTilesH);h++){const maxWByPanels=Math.max(1,Math.floor(maxPanelsPerProc/h)),maxWByPixels=Math.max(1,Math.floor(procCapPixels/(h*tilePixels))),maxW=Math.min(maxTilesW,maxWByPanels,maxWByPixels);if(maxW<=0)continue;const total=Math.ceil(wallTilesX/maxW)+dp[remaining-h];total<dp[remaining]&&(dp[remaining]=total,pick[remaining]=h)}if(!Number.isFinite(dp[wallTilesY]))return null;const bands=[];let yPos=0,remaining=wallTilesY;for(;remaining>0;){const h=pick[remaining],maxWByPanels=Math.max(1,Math.floor(maxPanelsPerProc/h)),maxWByPixels=Math.max(1,Math.floor(procCapPixels/(h*tilePixels))),maxW=Math.min(maxTilesW,maxWByPanels,maxWByPixels),cols=Math.ceil(wallTilesX/maxW),baseW=Math.floor(wallTilesX/cols),rem=wallTilesX%cols,regions=[];let xPos=0;for(let c=0;c<cols;c++){const w=baseW+(c<rem?1:0),panels=w*h,px=panels*tilePixels,xdPorts=Math.max(1,Math.ceil(panels/Math.max(1,panelsPerPort))),xds=Math.ceil(xdPorts/portsPerUnit);regions.push({x:xPos,y:yPos,w:w,h:h,panels:panels,pixels:px,xdPorts:xdPorts,xds:xds}),xPos+=w}bands.push({y:yPos,h:h,regions:regions}),yPos+=h,remaining-=h}const allRegions=bands.flatMap(band=>band.regions);return{totalProcessors:allRegions.length,totalXds:allRegions.reduce((acc,r)=>acc+r.xds,0),maxXdPerProc:Math.max(0,...allRegions.map(r=>r.xds)),avgUtil:allRegions.length>0?allRegions.reduce((acc,r)=>acc+r.pixels,0)/(allRegions.length*Math.max(1,procCapPixels))*100:0,bands:bands,allRegions:allRegions}}function calculateOptimalHardware(){const{wallTilesX:wallTilesX,wallTilesY:wallTilesY,tilePxW:tilePxW,tilePxH:tilePxH}=state.wall,tilePixels=tilePxW*tilePxH,totalPanels=wallTilesX*wallTilesY,wallPxW=wallTilesX*tilePxW,wallPxH=wallTilesY*tilePxH,canonicalProc=state.processors[0]||{profile:"Brompton SX40",capacityMode:"profile",latencyMode:"From Project"},resolved=resolveProcessorCapacity(canonicalProc,state.metadata),portData=resolvePortCapacityForProcessor(canonicalProc,state.metadata),procCapPixels=Math.floor(1e6*resolved.capacityMPix),refNode=state.nodes[0]||{maxWidth:7680,maxHeight:4320,maxMPix:14},inputGroup=getInputGroup(canonicalProc.profile,state.metadata.frameRate),nodeMaxW=inputGroup?Math.min(refNode.maxWidth,inputGroup.w):refNode.maxWidth,nodeMaxH=inputGroup?Math.min(refNode.maxHeight,inputGroup.h):refNode.maxHeight,nodeMaxW_rot=nodeMaxH,nodeMaxH_rot=nodeMaxW,maxTilesW=Math.max(1,Math.floor(nodeMaxW/Math.max(1,tilePxW))),maxTilesH=Math.max(1,Math.floor(nodeMaxH/Math.max(1,tilePxH)));if(tilePixels>portData.portPixels)return{error:`Panel resolution (${tilePxW}x${tilePxH} = ${formatInt(tilePixels)} px) exceeds ${portData.portUnitLabel} port capacity (${formatInt(portData.portPixels)} px/port) at these settings. Reduce frame rate, bit depth, or switch to Standard latency.`,feasible:!1,procCapMPix:resolved.capacityMPix,xdPortPixels:portData.portPixels};const panelsPerPort=Math.min(Math.floor(portData.portPixels/tilePixels),500),portsPerProcessor=Math.max(1,getPortsPerProcessorLimit(canonicalProc)),panelsByPixelCap=Math.max(1,Math.floor(procCapPixels/tilePixels)),panelsByPortCap=panelsPerPort*portsPerProcessor,maxPanelsPerSX40=Math.min(panelsByPixelCap,panelsByPortCap,2e3);if(maxPanelsPerSX40<=0)return{error:"Panel size exceeds processor/XD capacity at these settings.",feasible:!1};const uniformCandidates=[],naiveMinLandscape=Math.ceil(wallPxW/Math.max(1,nodeMaxW))*Math.ceil(wallPxH/Math.max(1,nodeMaxH)),naiveMinPortrait=Math.ceil(wallPxW/Math.max(1,nodeMaxW_rot))*Math.ceil(wallPxH/Math.max(1,nodeMaxH_rot)),naiveMin=Math.max(Math.ceil(totalPanels/maxPanelsPerSX40),Math.min(naiveMinLandscape,naiveMinPortrait));for(let n=naiveMin;n<=Math.min(4*naiveMin,64);n++){const pairs=factorPairs(n);for(const[cols,rows]of pairs){if(wallTilesX%cols!==0||wallTilesY%rows!==0)continue;const segTilesW=wallTilesX/cols,segTilesH=wallTilesY/rows,segPxW=segTilesW*tilePxW,segPxH=segTilesH*tilePxH,segPixels=segPxW*segPxH,segPanels=segTilesW*segTilesH,isPortrait=segPxH>segPxW;if(segPxW>(isPortrait?nodeMaxW_rot:nodeMaxW)||segPxH>(isPortrait?nodeMaxH_rot:nodeMaxH))continue;if(segPixels>procCapPixels)continue;if(segPanels>maxPanelsPerSX40)continue;const maxUnitsPerProc=Math.floor(portsPerProcessor/portData.portsPerUnit),xdPerProc=Math.ceil(segPanels/(panelsPerPort*portData.portsPerUnit));if(xdPerProc>maxUnitsPerProc)continue;const totalXds=n*xdPerProc,utilization=segPixels/procCapPixels*100,orientation=segPxH>segPxW?"portrait":segPxW>segPxH?"landscape":"square";uniformCandidates.push({type:"uniform",processors:n,cols:cols,rows:rows,segTilesW:segTilesW,segTilesH:segTilesH,segPxW:segPxW,segPxH:segPxH,segPanels:segPanels,xdPerProcessor:xdPerProc,totalXds:totalXds,utilization:utilization,orientation:orientation,panelsPerPort:panelsPerPort,totalPanels:totalPanels,maxPanelsPerSX40:maxPanelsPerSX40})}}const bandLayout=findOptimalBandLayout(wallTilesX,wallTilesY,maxTilesW,maxTilesH,maxPanelsPerSX40,panelsPerPort,procCapPixels,tilePixels,portData.portsPerUnit);let bandCandidate=null;const maxUnitsPerProcGlobal=Math.floor(portsPerProcessor/portData.portsPerUnit);bandLayout&&bandLayout.maxXdPerProc<=maxUnitsPerProcGlobal&&(bandCandidate={type:"band",processors:bandLayout.totalProcessors,totalXds:bandLayout.totalXds,avgUtil:bandLayout.avgUtil,bands:bandLayout.bands,allRegions:bandLayout.allRegions,panelsPerPort:panelsPerPort,totalPanels:totalPanels,maxPanelsPerSX40:maxPanelsPerSX40}),uniformCandidates.sort((a,b)=>{if(a.processors!==b.processors)return a.processors-b.processors;const aDelta=Math.abs(a.utilization-80),bDelta=Math.abs(b.utilization-80);return aDelta!==bDelta?aDelta-bDelta:a.totalXds-b.totalXds});const bestUniform=uniformCandidates[0]||null;let best=null,alternatives=[];if(bestUniform&&bandCandidate?bestUniform.processors<=bandCandidate.processors?(best=bestUniform,alternatives=uniformCandidates.slice(1,4)):(best=bandCandidate,bestUniform&&alternatives.push(bestUniform)):bestUniform?(best=bestUniform,alternatives=uniformCandidates.slice(1,4)):bandCandidate&&(best=bandCandidate),!best)return{error:`No feasible split found for ${wallTilesX}x${wallTilesY} wall at ${state.metadata.frameRate} Hz / ${state.metadata.colorDepth} / ${resolved.latencyMode}.`,feasible:!1,procCapMPix:resolved.capacityMPix,xdPortPixels:portData.portPixels};const portraitBlockReason=wallPxH>nodeMaxH_rot?`region height ${wallPxH}px exceeds node maxHeight in portrait mode (${nodeMaxH_rot}px)`:null,hasAnyLandscape=uniformCandidates.some(c=>"portrait"!==c.orientation)||!!bandCandidate;return{feasible:!0,best:best,alternatives:alternatives,allUniformCandidates:uniformCandidates,bandCandidate:bandCandidate,portraitBlockReason:portraitBlockReason,landscapeBlockReason:hasAnyLandscape?null:`wall tile height (${wallTilesY}) has no valid divisors for multi-row splits`,settings:{fps:state.metadata.frameRate,latency:resolved.latencyMode,depth:state.metadata.colorDepth,procCapMPix:resolved.capacityMPix,xdPortPixels:portData.portPixels,procLabel:(canonicalProc.profile||"Brompton SX40").split(" ").pop(),unitLabel:portData.portUnitLabel||"XD",parentUnit:PROCESSOR_REGISTRY[canonicalProc.profile]?.parentUnit||null},wallInfo:{totalPanels:totalPanels,wallPxW:wallPxW,wallPxH:wallPxH,tilePixels:tilePixels,nodeMaxW:nodeMaxW,nodeMaxH:nodeMaxH}}}function applyHardwareRecommendation(hw){if(!hw||!hw.feasible||!hw.best)return void renderLog("Cannot apply: no feasible hardware recommendation.");const b=hw.best,refNode=state.nodes[0]||{maxWidth:7680,maxHeight:4320,maxMPix:14,outputs:2};let regionRects;if("band"===b.type)regionRects=b.allRegions;else{regionRects=[];for(let row=0;row<b.rows;row++)for(let col=0;col<b.cols;col++)regionRects.push({x:col*b.segTilesW,y:row*b.segTilesH,w:b.segTilesW,h:b.segTilesH,panels:b.segPanels})}const count=regionRects.length,newProcessors=[];for(let i=0;i<count;i++){const name=i<26?`PROC-${String.fromCharCode(65+i)}`:`PROC-${i+1}`;newProcessors.push(defaultProcessor(name,state.metadata.processorProfile||"Brompton SX40"))}const newNodes=[];for(let i=0;i<count;i++)newNodes.push({id:uid("node"),name:`Render Node ${i+1}`,maxWidth:refNode.maxWidth,maxHeight:refNode.maxHeight,maxMPix:refNode.maxMPix,outputs:refNode.outputs,backupNodeId:""});const newRegions=regionRects.map((rect,i)=>({id:uid("reg"),name:`Region ${i+1}`,x:rect.x,y:rect.y,w:rect.w,h:rect.h,nodeId:newNodes[i].id,processorId:newProcessors[i].id,role:"Background"}));state.processors=newProcessors,state.nodes=newNodes,state.regions=newRegions,state.selection={type:"region",id:newRegions[0]?.id||""};const splitLabel="band"===b.type?`${b.bands.length} band(s), ${count} region(s)`:`${b.cols}x${b.rows} grid`;fitView(),renderAll(`Applied: ${count} ${(state.metadata.processorProfile||"Brompton SX40").split(" ").pop()}(s), ${b.totalXds} ${PROCESSOR_REGISTRY[state.metadata.processorProfile]?.portUnitLabel||"XD"}(s), ${splitLabel}`)}function renderDetails(report){"selection"!==state.activeTab?"wall"!==state.activeTab?renderDeploymentDetails(report):renderWallDetails(report):renderSelectionDetails(report)}function renderSelectionDetails(report){const sel=getSelectedObject();if(sel){if("node"===state.selection.type){const node=sel,backupOptions=['<option value="">None</option>',...state.nodes.filter(n=>n.id!==node.id).map(n=>`<option value="${n.id}" ${n.id===node.backupNodeId?"selected":""}>${escapeHtml(n.name)}</option>`)].join("");els.detailsHost.innerHTML=`\n      <div class="details-group">\n        <h3>Render Node</h3>\n        <label class="field" title="Identifier for this render machine">Name<input data-edit="node.name" value="${escapeAttr(node.name)}" /></label>\n        <div class="grid-2">\n          <label class="field" title="Maximum single output width supported by this node">Max Width<input type="number" data-edit="node.maxWidth" value="${node.maxWidth}" /></label>\n          <label class="field" title="Maximum single output height supported by this node">Max Height<input type="number" data-edit="node.maxHeight" value="${node.maxHeight}" /></label>\n          <label class="field" title="Physical output ports on this node - sets maximum render regions">Output Ports<input type="number" data-edit="node.outputs" value="${node.outputs}" min="1" max="8" /></label>\n          <div class="field" title="Max pixel throughput derived from Max Width x Max Height">Max MPix<br><strong>${(node.maxWidth*node.maxHeight/1e6).toFixed(2)} MPix</strong><br><span style="font-size:0.68rem;color:var(--muted)">${node.maxWidth}x${node.maxHeight}</span></div>\n        </div>\n        <label class="field" title="Optional failover node. Use this only when running redundancy (N+1 or mirror).">Failover Node (Optional)<select data-edit="node.backupNodeId">${backupOptions}</select></label>\n        <div class="toolbar-group">\n          <button class="tool-btn" data-action="copy-node-settings" title="Copy this node's sizing/output settings">Copy Node Settings</button>\n          <button class="tool-btn" data-action="paste-node-settings" title="Paste copied settings into this node" ${nodeSettingsClipboard?"":"disabled"}>Paste Settings</button>\n        </div>\n      </div>`}else if("processor"===state.selection.type){const processor=sel,pLoad=report.processorLoads[processor.id]||{capacityMPix:0,matchedFps:24,source:"Custom",xdPortCapacityPixels:0,xdPortsRequired:0,xdsRequired:0};els.detailsHost.innerHTML=`\n      <div class="details-group">\n        <h3>Processor</h3>\n        <label class="field" title="Identifier for LED processor/controller">Name<input data-edit="processor.name" value="${escapeAttr(processor.name)}" /></label>\n        <div class="field proc-profile-display" title="Processor type is set project-wide in Wall Settings">Type (project-wide)<span class="proc-profile-badge">${escapeHtml(state.metadata.processorProfile||"Brompton SX40")}</span></div>\n        <div class="grid-2">\n          ${selectField("Capacity Mode","processor.capacityMode",["profile","custom"],processor.capacityMode,"Use profile capacity or manual MPix cap")}\n          ${selectField("Latency Mode","processor.latencyMode",["From Project","Standard","ULL"],processor.latencyMode,"Latency mode for this processor")}\n          <label class="field" title="Manual processor cap in MPix (used in Custom mode)">Custom MPix<input type="number" step="0.1" data-edit="processor.customMPix" value="${processor.customMPix}" ${"custom"===processor.capacityMode?"":"disabled"} /></label>\n        </div>\n      </div>\n      <div class="details-group">\n        <h3>Computed Throughput</h3>\n        <div class="field">Resolved cap: <strong>${pLoad.capacityMPix.toFixed(3)} MPix/frame</strong></div>\n        <div class="field">Source: ${escapeHtml(pLoad.source)} (fps matched: ${pLoad.matchedFps}, mode: ${escapeHtml(pLoad.latencyMode)})</div>\n        <div class="field">Project context: ${state.metadata.frameRate} fps / ${state.metadata.colorDepth} / ${state.metadata.latencyMode}</div>\n        ${(()=>{const reg=PROCESSOR_REGISTRY[processor.profile],unitLabel=pLoad.portUnitLabel||"XD",ppu=pLoad.portsPerUnit||10,maxUnits=reg?reg.maxUnits===1/0?"∞":String(reg.maxUnits):String(4),hardCap=reg?.hardCapMPix?`<div class="field">Hard cap: <strong>${reg.hardCapMPix} MPix/frame</strong> per unit</div>`:"",infoNote=reg?.infoNote?`<div class="field proc-info-note">${escapeHtml(reg.infoNote)}</div>`:"";return`\n        <div class="field">${unitLabel} port capacity (1G): ${formatInt(pLoad.xdPortCapacityPixels)} px/port</div>\n        <div class="field">${unitLabel} required: ${pLoad.xdPortsRequired} ports → ${pLoad.xdsRequired} ${unitLabel}(s) @ ${ppu} ports each</div>\n        <div class="field">Max ${unitLabel} per processor: ${maxUnits}</div>${hardCap}${infoNote}`})()}\n      </div>`}else if("region"===state.selection.type){const region=sel,nodeOpts=['<option value="">Unassigned</option>',...state.nodes.map(n=>`<option value="${n.id}" ${n.id===region.nodeId?"selected":""}>${escapeHtml(n.name)}</option>`)].join(""),procOpts=['<option value="">Unassigned</option>',...state.processors.map(p=>`<option value="${p.id}" ${p.id===region.processorId?"selected":""}>${escapeHtml(p.name)}</option>`)].join(""),roleOpts=roles.map(role=>`<option value="${role}" ${region.role===role?"selected":""}>${role}</option>`).join("");els.detailsHost.innerHTML=`\n      <div class="details-group">\n        <h3>Render Area</h3>\n        <label class="field" title="Name of this mapped wall area">Name<input data-edit="region.name" value="${escapeAttr(region.name)}" /></label>\n        <div class="grid-2">\n          <label class="field" title="X origin in cabinet tiles">Coord X<input type="number" data-edit="region.x" value="${region.x}" /></label>\n          <label class="field" title="Y origin in cabinet tiles">Coord Y<input type="number" data-edit="region.y" value="${region.y}" /></label>\n          <label class="field" title="Width in cabinet tiles">W (tiles)<input type="number" data-edit="region.w" value="${region.w}" /></label>\n          <label class="field" title="Height in cabinet tiles">H (tiles)<input type="number" data-edit="region.h" value="${region.h}" /></label>\n        </div>\n        <label class="field" title="Render node responsible for this area"><select data-edit="region.nodeId" class="${region.nodeId?"":"unassigned-select"}">${nodeOpts}</select></label>\n        <label class="field" title="Processor responsible for this area"><select data-edit="region.processorId" class="${region.processorId?"":"unassigned-select"}">${procOpts}</select></label>\n        <label class="field" title="Logical purpose"><select data-edit="region.role">${roleOpts}</select></label>\n        <div class="toolbar-group">\n          <button class="tool-btn" data-action="smart-size-region" title="Auto-size this region to use processor capacity efficiently">Smart Size Region</button>\n        </div>\n      </div>`}bindDetailsEditors()}else els.detailsHost.innerHTML='<div class="details-group"><h3>No selection</h3><div class="field">Select a node, processor, or render area from outliner or canvas.</div></div>'}function renderWallDetails(report){const w=state.wall;els.detailsHost.innerHTML=`\n    <div class="details-group">\n      <h3>Wall Geometry</h3>\n      <div class="grid-2">\n        <label class="field" title="Total cabinets horizontally">Wall Tiles X<input type="number" data-edit="wall.wallTilesX" value="${w.wallTilesX}" /></label>\n        <label class="field" title="Total cabinets vertically">Wall Tiles Y<input type="number" data-edit="wall.wallTilesY" value="${w.wallTilesY}" /></label>\n        <label class="field" title="Pixel width per cabinet">Cabinet Width px<input type="number" data-edit="wall.tilePxW" value="${w.tilePxW}" /></label>\n        <label class="field" title="Pixel height per cabinet">Cabinet Height px<input type="number" data-edit="wall.tilePxH" value="${w.tilePxH}" /></label>\n        <label class="field" title="Physical width in millimeters per cabinet">Cabinet Width mm<input type="number" data-edit="wall.tileMmW" value="${w.tileMmW}" /></label>\n        <label class="field" title="Physical height in millimeters per cabinet">Cabinet Height mm<input type="number" data-edit="wall.tileMmH" value="${w.tileMmH}" /></label>\n      </div>\n      <div class="grid-2">\n        <label class="field" title="Capacity guardrail subtracted from node caps">Safety Margin %<input type="number" data-edit="wall.safetyMargin" value="${w.safetyMargin}" /></label>\n        <label class="field" title="Warning threshold for node load">Target Util %<input type="number" data-edit="wall.targetUtil" value="${w.targetUtil}" /></label>\n      </div>\n    </div>\n    <div class="details-group">\n      <h3>Computed</h3>\n      <div class="field">Canvas: ${report.wall.wallPxW}x${report.wall.wallPxH} px</div>\n      <div class="field">Total Pixels: ${formatInt(report.wall.wallTotalPixels)} (${report.wall.wallTotalMPix.toFixed(2)} MPix)</div>\n      <div class="field">Physical: ${(report.wall.wallMmW/1e3).toFixed(2)}m x ${(report.wall.wallMmH/1e3).toFixed(2)}m</div>\n      <div class="field">Pitch: ${report.wall.pixelPitchW.toFixed(2)}mm x ${report.wall.pixelPitchH.toFixed(2)}mm</div>\n    </div>`,bindDetailsEditors()}function renderDeploymentDetails(report){const summary=deploymentSummary(report);els.detailsHost.innerHTML=`\n    <div class="details-group">\n      <h3>Video / Sync</h3>\n      <label class="field" title="Project label"><input data-edit="meta.projectName" value="${escapeAttr(state.metadata.projectName)}" /></label>\n      <label class="field" title="Build/revision tag"><input data-edit="meta.buildTag" value="${escapeAttr(state.metadata.buildTag)}" /></label>\n      <div class="grid-2">\n        ${selectField("Frame Rate","meta.frameRate",FPS_OPTIONS,state.metadata.frameRate,"Global frame rate")}\n        ${selectField("Color Depth","meta.colorDepth",["8-bit","10-bit","12-bit"],state.metadata.colorDepth,"Global color depth")}\n        ${selectField("Color Space","meta.colorSpace",["Rec.709","DCI-P3","Rec.2020"],state.metadata.colorSpace,"Global gamut")}\n        ${selectField("EOTF","meta.eotf",["Gamma 2.2","Gamma 2.4","PQ (ST.2084)"],state.metadata.eotf,"Transfer function")}\n        ${selectField("Sync","meta.syncMode",["Genlock","Free Run"],state.metadata.syncMode,"Frame sync mode")}\n        ${selectField("Redundancy","meta.redundancy",["None","N+1","Full Mirror"],state.metadata.redundancy,"Failover strategy")}\n        ${selectField("Latency","meta.latencyMode",["Standard","ULL"],state.metadata.latencyMode,"Project latency mode")}\n      </div>\n      <label class="field">Genlock Source<input data-edit="meta.genlockSource" value="${escapeAttr(state.metadata.genlockSource)}" /></label>\n      <label class="field">Control Network<input data-edit="meta.controlNetwork" value="${escapeAttr(state.metadata.controlNetwork)}" /></label>\n    </div>\n    <div class="details-group">\n      <h3>Deployment Summary</h3>\n      <label class="field"><textarea rows="14" readonly>${escapeHtml(summary)}</textarea></label>\n    </div>`,bindDetailsEditors()}function bindDetailsEditors(){els.detailsHost.querySelectorAll("[data-edit]").forEach(input=>{input.addEventListener("input",onDetailsEdit),input.addEventListener("change",onDetailsEdit)}),els.detailsHost.querySelectorAll("[data-action]").forEach(btn=>{btn.addEventListener("click",onDetailsAction)})}function onDetailsEdit(e){const path=e.target.dataset.edit,value=e.target.value;if(path.startsWith("node.")){const node=getSelectedObject("node");if(!node)return;setField(node,path.slice(5),value)}else if(path.startsWith("processor.")){const processor=getSelectedObject("processor");if(!processor)return;setField(processor,path.slice(10),value),sanitizeProcessor(processor)}else if(path.startsWith("region.")){const region=getSelectedObject("region");if(!region)return;setField(region,path.slice(7),value),clampRegion(region)}else path.startsWith("wall.")?(setField(state.wall,path.slice(5),value),sanitizeWall()):path.startsWith("meta.")&&setField(state.metadata,path.slice(5),value);"input"!==e.type?renderAll("Updated details"):renderAll(void 0,{skipDetails:!0})}function onDetailsAction(e){const action=e.currentTarget.dataset.action;if("copy-node-settings"===action){const node=getSelectedObject("node");if(!node)return;return nodeSettingsClipboard={maxWidth:node.maxWidth,maxHeight:node.maxHeight,maxMPix:node.maxMPix,outputs:node.outputs},void renderAll("Copied node settings")}if("paste-node-settings"===action){const node=getSelectedObject("node");if(!node||!nodeSettingsClipboard)return;return node.maxWidth=nodeSettingsClipboard.maxWidth,node.maxHeight=nodeSettingsClipboard.maxHeight,node.maxMPix=nodeSettingsClipboard.maxMPix,node.outputs=nodeSettingsClipboard.outputs,void renderAll(`Pasted settings into ${node.name}`)}if("smart-size-region"===action){const region=getSelectedObject("region");if(!region)return;const smart=suggestSmartPlacementForProcessor(region.processorId,region.id);region.x=smart.x,region.y=smart.y,region.w=smart.w,region.h=smart.h,clampRegion(region),renderAll(`Smart-sized ${region.name}`)}}function suggestSmartPlacementForProcessor(processorId,ignoreRegionId=""){const tilePxW=Math.max(1,state.wall.tilePxW),tilePxH=Math.max(1,state.wall.tilePxH),tilePixels=tilePxW*tilePxH,processor=state.processors.find(p=>p.id===processorId)||defaultProcessor("tmp"),resolved=resolveProcessorCapacity(processor,state.metadata),portData=resolvePortCapacityForProcessor(processor,state.metadata),procCapPixels=Math.floor(1e6*resolved.capacityMPix);if(tilePixels>portData.portPixels)return{x:0,y:0,w:1,h:1};const maxPanelsByPorts=Math.min(Math.floor(portData.portPixels/tilePixels),500)*getPortsPerProcessorLimit(processor),maxPanelsByPixels=Math.max(1,Math.floor(procCapPixels/tilePixels)),maxPanels=Math.min(maxPanelsByPixels,maxPanelsByPorts,2e3),region=state.regions.find(r=>r.id===ignoreRegionId),node=region?state.nodes.find(n=>n.id===region.nodeId):state.nodes[0],rawNodeMaxW=node?node.maxWidth:7680,rawNodeMaxH=node?node.maxHeight:4320,ig=getInputGroup(processor.profile,state.metadata.frameRate),igMaxDim=ig?ig.w:1/0,nodeMaxW=Math.min(rawNodeMaxW,igMaxDim),nodeMaxH=Math.min(rawNodeMaxH,igMaxDim),maxTilesW=Math.min(state.wall.wallTilesX,Math.max(1,Math.floor(nodeMaxW/tilePxW))),maxTilesH=Math.min(state.wall.wallTilesY,Math.max(1,Math.floor(nodeMaxH/tilePxH))),aspect=state.wall.wallTilesX/Math.max(1,state.wall.wallTilesY);let bestW=1,bestH=1;for(let h=1;h<=maxTilesH;h++){const rPxH=h*tilePxH;let igWCap=1/0;if(ig)if(rPxH<=ig.h)igWCap=Math.floor(ig.w/tilePxW);else{if(!(rPxH<=ig.w))continue;igWCap=Math.floor(ig.h/tilePxW)}const wByPanels=Math.floor(maxPanels/h),wByPixels=Math.floor(procCapPixels/(h*tilePixels)),w=Math.min(maxTilesW,wByPanels,wByPixels,igWCap);w<1||(w*h>bestW*bestH||w*h===bestW*bestH&&Math.abs(w/h-aspect)<Math.abs(bestW/bestH-aspect))&&(bestW=w,bestH=h)}let found=findOpenSlot(bestW,bestH,ignoreRegionId);if(!found)for(let h=bestH;h>=1&&!found;h--)for(let w=Math.min(bestW,Math.floor(maxPanels/h));w>=1&&!found;w--)w*h*tilePixels>procCapPixels||(found=findOpenSlot(w,h,ignoreRegionId));return found||(found={x:0,y:0,w:bestW,h:bestH}),found}function findOpenSlot(w,h,ignoreRegionId=""){const regions=state.regions.filter(r=>r.id!==ignoreRegionId);for(let y=0;y<=state.wall.wallTilesY-h;y+=1)for(let x=0;x<=state.wall.wallTilesX-w;x+=1){const probe={x:x,y:y,w:w,h:h};if(!regions.some(r=>rectOverlap(probe,r)))return probe}return null}function sanitizeWall(){state.wall.wallTilesX=clampInt(state.wall.wallTilesX,1,400),state.wall.wallTilesY=clampInt(state.wall.wallTilesY,1,200),state.wall.tilePxW=clampInt(state.wall.tilePxW,1,5e3),state.wall.tilePxH=clampInt(state.wall.tilePxH,1,5e3),state.wall.tileMmW=clampInt(state.wall.tileMmW,1,2e3),state.wall.tileMmH=clampInt(state.wall.tileMmH,1,2e3),state.wall.safetyMargin=clampFloat(state.wall.safetyMargin,0,50),state.wall.targetUtil=clampFloat(state.wall.targetUtil,20,100),state.regions.forEach(clampRegion)}function sanitizeProcessor(processor){processor.customMPix=clampFloat(processor.customMPix,.1,1e3),PROCESSOR_PROFILES.includes(processor.profile)||(processor.profile="Custom"),["profile","custom"].includes(processor.capacityMode)||(processor.capacityMode="custom"),["From Project","Standard","ULL"].includes(processor.latencyMode)||(processor.latencyMode="From Project")}function setField(obj,key,raw){["x","y","w","h","maxWidth","maxHeight","outputs","wallTilesX","wallTilesY","tilePxW","tilePxH","tileMmW","tileMmH"].includes(key)?obj[key]=clampInt(raw,0,1e5):["maxMPix","safetyMargin","targetUtil","customMPix"].includes(key)?obj[key]=clampFloat(raw,0,1e5):obj[key]=raw}function renderCanvas(report){resizeCanvas();const ctx=els.canvas.getContext("2d");if(!ctx)return;ctx.clearRect(0,0,els.canvas.width,els.canvas.height);const origin=tileToScreen(0,0),wallW=state.wall.wallTilesX*view.zoom,wallH=state.wall.wallTilesY*view.zoom;ctx.fillStyle="#0b1525",ctx.fillRect(origin.x,origin.y,wallW,wallH),drawGrid(ctx),state.regions.forEach((region,idx)=>{const unassigned=!region.nodeId,c=unassigned?"#888899":colorForRegion(region,idx),p=tileToScreen(region.x,region.y),rw=region.w*view.zoom,rh=region.h*view.zoom,selected=isSelected("region",region.id);if(ctx.fillStyle=hexToRgba(c,selected?.42:.18),ctx.strokeStyle=selected?"#ffffff":c,ctx.lineWidth=selected?2.2:1.4,ctx.fillRect(p.x,p.y,rw,rh),unassigned){ctx.save(),ctx.beginPath(),ctx.rect(p.x,p.y,rw,rh),ctx.clip(),ctx.strokeStyle="rgba(160, 160, 180, 0.35)",ctx.lineWidth=1;const spacing=Math.max(6,.45*view.zoom),diag=rw+rh;for(let d=-rh;d<rw;d+=spacing)ctx.beginPath(),ctx.moveTo(p.x+d,p.y),ctx.lineTo(p.x+d+diag,p.y+diag),ctx.stroke();ctx.restore()}ctx.strokeRect(p.x,p.y,rw,rh),ctx.fillStyle=unassigned?"#aaaacc":"#edf4ff",ctx.font='12px "Roboto Mono", monospace';const label=`${region.name} ${region.w*state.wall.tilePxW}x${region.h*state.wall.tilePxH}`;ctx.fillText(label,p.x+5,p.y+14),selected&&drawHandles(ctx,region)}),view.showCableOverlay&&report.cablePlan&&drawCableOverlay(ctx,report.cablePlan),ctx.strokeStyle="#6b8fca",ctx.lineWidth=1.5,ctx.strokeRect(origin.x,origin.y,wallW,wallH);const wallPxW=report.wall.wallPxW,wallPxH=report.wall.wallPxH,wallMmW=report.wall.wallMmW,wallMmH=report.wall.wallMmH,dimLabel=`${wallPxW}x${wallPxH}px  |  ${(wallMmW/1e3).toFixed(2)}m x ${(wallMmH/1e3).toFixed(2)}m`;ctx.save(),ctx.font='bold 11px "Roboto Mono", monospace';const textW=ctx.measureText(dimLabel).width,labelX=origin.x+(wallW-textW)/2,labelY=origin.y+wallH+16;labelY<els.canvas.height-4&&(ctx.fillStyle="rgba(10,12,20,0.65)",ctx.fillRect(labelX-6,labelY-12,textW+12,18),ctx.fillStyle="#9eb8e8",ctx.fillText(dimLabel,labelX,labelY)),ctx.restore();const coverage=report.wall.coveragePct.toFixed(1),selectedRegion=getSelectedObject("region"),selectText=selectedRegion?`${selectedRegion.name} @ ${selectedRegion.x},${selectedRegion.y} ${selectedRegion.w}x${selectedRegion.h}`:"none";els.canvasStatus.textContent=`Zoom ${view.zoom.toFixed(1)} px/tile | Coverage ${coverage}% | Selected ${selectText}`}function drawGrid(ctx){const start=tileToScreen(0,0);ctx.strokeStyle="rgba(143, 175, 228, 0.22)",ctx.lineWidth=1;for(let x=1;x<state.wall.wallTilesX;x+=1){const px=start.x+x*view.zoom;ctx.beginPath(),ctx.moveTo(px,start.y),ctx.lineTo(px,start.y+state.wall.wallTilesY*view.zoom),ctx.stroke()}for(let y=1;y<state.wall.wallTilesY;y+=1){const py=start.y+y*view.zoom;ctx.beginPath(),ctx.moveTo(start.x,py),ctx.lineTo(start.x+state.wall.wallTilesX*view.zoom,py),ctx.stroke()}}function drawCableOverlay(ctx,cablePlan){if(!cablePlan?.processorPlans?.length)return;let globalXdOffset=0;cablePlan.processorPlans.forEach(procPlan=>{procPlan.xds.forEach(xd=>{const displayXdIdx=view.globalUnitNumbering?globalXdOffset+xd.index:xd.index;xd.ports.forEach(port=>{if(!port.segments?.length)return;const tilePath=buildPortTilePath(port),stepCount=tilePath.length;if(tilePath.forEach((tile,idx)=>{const color=cableFillColorForStep(tile.base,port.index,idx,stepCount,.62);ctx.fillStyle=color;const p=tileToScreen(tile.x,tile.y);ctx.fillRect(p.x+1,p.y+1,Math.max(0,view.zoom-2),Math.max(0,view.zoom-2))}),view.zoom>=24&&tilePath.length){const p=tileToScreen(tilePath[0].x,tilePath[0].y);ctx.fillStyle="rgba(255,255,255,0.85)",ctx.font='10px "Roboto Mono", monospace',ctx.fillText(`${procPlan.portUnitPrefix||"X"}${displayXdIdx}P${port.index}`,p.x+3,p.y+11)}drawPortDaisyArrows(ctx,tilePath,port.index)})}),globalXdOffset+=procPlan.xds.length})}function buildPortTilePath(port){const tilePath=[];return(port.segments||[]).forEach(seg=>{const region=state.regions.find(r=>r.id===seg.regionId),regionIndex=region?Math.max(0,state.regions.findIndex(r=>r.id===region.id)):0,base=region?colorForRegion(region,regionIndex):"#6cb0ff";if(seg.vertical){const start=seg.reversed?seg.h-1:0,end=seg.reversed?-1:seg.h,step=seg.reversed?-1:1;for(let ty=start;ty!==end;ty+=step)tilePath.push({x:seg.x,y:seg.y+ty,base:base})}else{const start=seg.reversed?seg.w-1:0,end=seg.reversed?-1:seg.w,step=seg.reversed?-1:1;for(let tx=start;tx!==end;tx+=step)tilePath.push({x:seg.x+tx,y:seg.y,base:base})}}),tilePath}function drawPortDaisyArrows(ctx,tilePath,portIndex){if(view.zoom<10)return;if(tilePath.length<2)return;const stepCount=tilePath.length-1;for(let i=0;i<stepCount;i+=1){const stroke=cableArrowColorForStep(tilePath[i].base,portIndex,i,stepCount,.9);drawArrowBetweenTiles(ctx,tilePath[i],tilePath[i+1],stroke)}}function drawArrowBetweenTiles(ctx,fromTile,toTile,stroke){const p1=tileToScreen(fromTile.x+.5,fromTile.y+.5),p2=tileToScreen(toTile.x+.5,toTile.y+.5);drawArrowLine(ctx,p1.x,p1.y,p2.x,p2.y,stroke)}function drawArrowLine(ctx,x1,y1,x2,y2,stroke){const dx=x2-x1,dy=y2-y1,len=Math.hypot(dx,dy);if(len<1)return;const ux=dx/len,uy=dy/len,pad=Math.min(8,Math.max(2,.15*view.zoom)),sx=x1+ux*pad,sy=y1+uy*pad,ex=x2-ux*pad,ey=y2-uy*pad;ctx.save(),ctx.strokeStyle=stroke,ctx.fillStyle=stroke,ctx.lineWidth=Math.max(1.2,Math.min(2.8,.045*view.zoom)),ctx.beginPath(),ctx.moveTo(sx,sy),ctx.lineTo(ex,ey),ctx.stroke();const head=Math.max(5,Math.min(10,.12*view.zoom)),angle=Math.atan2(ey-sy,ex-sx);ctx.beginPath(),ctx.moveTo(ex,ey),ctx.lineTo(ex-head*Math.cos(angle-Math.PI/7),ey-head*Math.sin(angle-Math.PI/7)),ctx.lineTo(ex-head*Math.cos(angle+Math.PI/7),ey-head*Math.sin(angle+Math.PI/7)),ctx.closePath(),ctx.fill(),ctx.restore()}function drawHandles(ctx,region){const size=Math.max(6,Math.min(10,.25*view.zoom)),corners={nw:{x:region.x,y:region.y},ne:{x:region.x+region.w,y:region.y},sw:{x:region.x,y:region.y+region.h},se:{x:region.x+region.w,y:region.y+region.h}};ctx.fillStyle="#ffffff",handles.forEach(h=>{const p=tileToScreen(corners[h].x,corners[h].y);ctx.fillRect(p.x-size/2,p.y-size/2,size,size)})}function onPointerDown(e){const hit=hitTest(e.offsetX,e.offsetY);if(2===e.button||1===e.button||0===e.button&&e.altKey)return view.dragging=!0,view.dragMode="pan",view.panStart={x:e.clientX,y:e.clientY,panX:view.panX,panY:view.panY},els.canvas.style.cursor="grabbing",void renderAll("Pan start");if("handle"===hit?.type){view.dragging=!0,view.dragMode="resize",view.dragRegionId=hit.regionId,view.dragHandle=hit.handle;const reg=state.regions.find(r=>r.id===hit.regionId);return view.dragStart={region:{...reg},screenX:e.offsetX,screenY:e.offsetY},select("region",hit.regionId),void renderAll("Resize start")}if("region"===hit?.type){view.dragging=!0,view.dragMode="move",view.dragRegionId=hit.regionId;const reg=state.regions.find(r=>r.id===hit.regionId),tile=screenToTile(e.offsetX,e.offsetY);return view.dragStart={region:{...reg},tileStart:tile},select("region",hit.regionId),void renderAll("Move start")}const t=screenToTile(e.offsetX,e.offsetY);inWall(t.x,t.y)&&(view.dragging=!0,view.dragMode="pan",view.panStart={x:e.clientX,y:e.clientY,panX:view.panX,panY:view.panY},els.canvas.style.cursor="grabbing",state.selection={type:"none",id:""},renderAll("Pan start"))}function updateCanvasCursor(e){if(view.dragging)return void(els.canvas.style.cursor="pan"===view.dragMode?"grabbing":"default");const hit=hitTest(e.offsetX,e.offsetY);if("handle"===hit?.type){const resizeCursors={nw:"nw-resize",ne:"ne-resize",sw:"sw-resize",se:"se-resize"};els.canvas.style.cursor=resizeCursors[hit.handle]||"nwse-resize"}else if("region"===hit?.type)els.canvas.style.cursor="grab";else{const t=screenToTile(e.offsetX,e.offsetY);els.canvas.style.cursor=inWall(t.x,t.y)?"grab":"default"}}function onPointerMove(e){if(view.dragging){if("pan"===view.dragMode&&view.panStart)return view.panX=view.panStart.panX+(e.clientX-view.panStart.x),view.panY=view.panStart.panY+(e.clientY-view.panStart.y),void renderCanvas(calculateReport());if("move"===view.dragMode){const region=state.regions.find(r=>r.id===view.dragRegionId);if(!region||!view.dragStart)return;const now=screenToTileFromClient(e.clientX,e.clientY),dx=now.x-view.dragStart.tileStart.x,dy=now.y-view.dragStart.tileStart.y;return region.x=view.dragStart.region.x+dx,region.y=view.dragStart.region.y+dy,clampRegion(region),void renderAll()}if("resize"===view.dragMode){const region=state.regions.find(r=>r.id===view.dragRegionId);if(!region||!view.dragStart)return;const now=screenToTileFromClient(e.clientX,e.clientY);resizeByHandle(region,view.dragStart.region,view.dragHandle,now),clampRegion(region),renderAll()}}else updateCanvasCursor(e)}function onPointerUp(){view.dragging&&view.dragMode&&renderAll(`Interaction complete (${view.dragMode})`),view.dragging=!1,view.dragMode="",view.dragHandle="",view.dragRegionId="",view.dragStart=null,view.panStart=null,els.canvas.style.cursor="default"}function onWheel(e){e.preventDefault();const before=screenToTile(e.offsetX,e.offsetY),factor=e.deltaY>0?.92:1.08;view.zoom=clampFloat(view.zoom*factor,7,180);const after=tileToScreen(before.x,before.y);view.panX+=e.offsetX-after.x,view.panY+=e.offsetY-after.y,renderCanvas(calculateReport())}function resizeByHandle(region,base,handle,tile){const maxX=state.wall.wallTilesX,maxY=state.wall.wallTilesY;let x1=base.x,y1=base.y,x2=base.x+base.w,y2=base.y+base.h;handle.includes("n")&&(y1=clampInt(tile.y,0,y2-1)),handle.includes("s")&&(y2=clampInt(tile.y,y1+1,maxY)),handle.includes("w")&&(x1=clampInt(tile.x,0,x2-1)),handle.includes("e")&&(x2=clampInt(tile.x,x1+1,maxX)),region.x=x1,region.y=y1,region.w=Math.max(1,x2-x1),region.h=Math.max(1,y2-y1)}function hitTest(screenX,screenY){const tile=screenToTile(screenX,screenY),selected=getSelectedObject("region");if(selected)for(const handle of handles)if(hitHandle(selected,handle,screenX,screenY))return{type:"handle",regionId:selected.id,handle:handle};for(let i=state.regions.length-1;i>=0;i-=1){const region=state.regions[i];if(tile.x>=region.x&&tile.x<region.x+region.w&&tile.y>=region.y&&tile.y<region.y+region.h)return{type:"region",regionId:region.id}}return null}function hitHandle(region,handle,screenX,screenY){const size=Math.max(6,Math.min(12,.25*view.zoom)),c={nw:tileToScreen(region.x,region.y),ne:tileToScreen(region.x+region.w,region.y),sw:tileToScreen(region.x,region.y+region.h),se:tileToScreen(region.x+region.w,region.y+region.h)}[handle];return Math.abs(screenX-c.x)<=size&&Math.abs(screenY-c.y)<=size}function renderIssues(issues){els.issues.innerHTML=issues.map(item=>`<div class="issue ${item.level}">${escapeHtml(item.message)}</div>`).join("")}function calculateReport(){const w=state.wall,wallPxW=w.wallTilesX*w.tilePxW,wallPxH=w.wallTilesY*w.tilePxH,wallTotalPixels=wallPxW*wallPxH,wallTotalMPix=wallTotalPixels/1e6,wallMmW=w.wallTilesX*w.tileMmW,wallMmH=w.wallTilesY*w.tileMmH,pixelPitchW=w.tileMmW/w.tilePxW,pixelPitchH=w.tileMmH/w.tilePxH,nodeLoads=Object.fromEntries(state.nodes.map(n=>[n.id,{node:n,regions:[],totalPixels:0,maxRegionW:0,maxRegionH:0}])),processorLoads=Object.fromEntries(state.processors.map(p=>{const resolved=resolveProcessorCapacity(p,state.metadata),portData=resolvePortCapacityForProcessor(p,state.metadata);return[p.id,{processor:p,regions:[],totalPixels:0,capacityMPix:resolved.capacityMPix,matchedFps:resolved.matchedFps,source:resolved.source,latencyMode:resolved.latencyMode,xdPortCapacityPixels:portData.portPixels,xdFps:portData.matchedFps,xdBpc:portData.bpc,portUnitLabel:portData.portUnitLabel,portsPerUnit:portData.portsPerUnit,xdPortsRequired:0,xdsRequired:0}]})),issues=[];for(const region of state.regions){const pxW=region.w*w.tilePxW,pxH=region.h*w.tilePxH,px=pxW*pxH;if((region.x<0||region.y<0||region.w<=0||region.h<=0)&&issues.push(bad(`Region "${region.name}" has invalid size.`)),(region.x+region.w>w.wallTilesX||region.y+region.h>w.wallTilesY)&&issues.push(bad(`Region "${region.name}" exceeds wall bounds.`)),region.nodeId){if(nodeLoads[region.nodeId]){const load=nodeLoads[region.nodeId];load.totalPixels+=px,load.regions.push(region),load.maxRegionW=Math.max(load.maxRegionW,pxW),load.maxRegionH=Math.max(load.maxRegionH,pxH)}}else issues.push(warn(`Region "${region.name}" is not assigned to a render node.`));if(region.processorId){if(processorLoads[region.processorId]){const load=processorLoads[region.processorId];load.totalPixels+=px,load.regions.push(region)}}else issues.push(warn(`Region "${region.name}" is not assigned to a processor.`))}for(let i=0;i<state.regions.length;i+=1)for(let j=i+1;j<state.regions.length;j+=1)rectOverlap(state.regions[i],state.regions[j])&&issues.push(bad(`Overlap between "${state.regions[i].name}" and "${state.regions[j].name}".`));const covered=new Set;for(const region of state.regions)for(let y=region.y;y<region.y+region.h;y+=1)for(let x=region.x;x<region.x+region.w;x+=1)inWall(x,y)&&covered.add(`${x},${y}`);const totalTiles=w.wallTilesX*w.wallTilesY,coveragePct=totalTiles>0?covered.size/totalTiles*100:0;coveragePct<100&&issues.push(warn(`Coverage is ${coveragePct.toFixed(1)}%. ${totalTiles-covered.size} tile(s) are unassigned.`)),Object.values(nodeLoads).forEach(load=>{const n=load.node,used=load.totalPixels/1e6,safeCap=n.maxMPix*(1-w.safetyMargin/100);(load.maxRegionW>n.maxWidth||load.maxRegionH>n.maxHeight)&&issues.push(bad(`${n.name} has region output exceeding ${n.maxWidth}x${n.maxHeight}.`)),used>safeCap?issues.push(bad(`${n.name} exceeds safe cap (${used.toFixed(2)} > ${safeCap.toFixed(2)} MPix).`)):used>n.maxMPix*(w.targetUtil/100)&&issues.push(warn(`${n.name} is over target utilization (${used.toFixed(2)} MPix).`)),load.regions.length>4&&issues.push(bad(`${n.name} has ${load.regions.length} regions assigned; max supported is 4.`))}),Object.values(processorLoads).forEach(load=>{const p=load.processor,used=load.totalPixels/1e6,perPort=Math.max(1,load.xdPortCapacityPixels),tilePixels=Math.max(1,w.tilePxW*w.tilePxH),totalPanels=load.regions.reduce((acc,r)=>acc+r.w*r.h,0),perPortPanelByPixels=Math.max(1,Math.floor(perPort/tilePixels)),panelCapPerPort=Math.max(1,Math.min(500,perPortPanelByPixels)),portsPerProcessorLimit=getPortsPerProcessorLimit(p),portsByPixels=Math.ceil(load.totalPixels/perPort),portsByPanels=Math.ceil(totalPanels/panelCapPerPort);load.portPanelCap=panelCapPerPort;const ppu=load.portsPerUnit||10,unitLabel=load.portUnitLabel||"XD";load.xdPortsRequired=Math.max(portsByPixels,portsByPanels),load.xdsRequired=Math.ceil(load.xdPortsRequired/ppu),load.portsPerProcessorLimit=portsPerProcessorLimit,tilePixels>perPort&&issues.push(bad(`${p.name}: panel (${formatInt(tilePixels)} px) exceeds ${unitLabel} port capacity (${formatInt(perPort)} px/port). This configuration cannot be cabled.`));const ig=getInputGroup(p.profile,state.metadata.frameRate);if(ig&&load.regions.forEach(r=>{const rPxW=r.w*w.tilePxW,rPxH=r.h*w.tilePxH,fitsLandscape=rPxW<=ig.w&&rPxH<=ig.h,fitsPortrait=rPxH<=ig.w&&rPxW<=ig.h;fitsLandscape||fitsPortrait||issues.push(bad(`${p.name} → ${r.name}: region ${rPxW}x${rPxH} exceeds ${p.profile} input group ${ig.label} (${ig.w}x${ig.h} @ ≤${ig.maxHz}Hz) in both orientations.`))}),used>load.capacityMPix&&issues.push(bad(`${p.name} exceeds processing cap (${used.toFixed(2)} > ${load.capacityMPix.toFixed(2)} MPix).`)),load.regions.length>1&&issues.push(bad(`${p.name} has ${load.regions.length} regions assigned; policy is 1 region per processor.`)),load.xdPortsRequired>portsPerProcessorLimit){const overPorts=load.xdPortsRequired-portsPerProcessorLimit,maxPanelsAtPorts=panelCapPerPort*portsPerProcessorLimit;issues.push(warn(`${p.name} requires ${load.xdPortsRequired} ports (${load.xdsRequired} ${unitLabel}), limit is ${portsPerProcessorLimit} ports (${Math.ceil(portsPerProcessorLimit/ppu)} ${unitLabel}). Over by ${overPorts} ports. Max panels at current settings: ${maxPanelsAtPorts}.`))}}),issues.length||issues.push(good("Validation passed. Current setup is within declared constraints."));const cablePlan=buildCablePlan({processorLoads:processorLoads,wall:w});return{wall:{wallTilesX:w.wallTilesX,wallTilesY:w.wallTilesY,wallPxW:wallPxW,wallPxH:wallPxH,wallTotalPixels:wallTotalPixels,wallTotalMPix:wallTotalMPix,wallMmW:wallMmW,wallMmH:wallMmH,pixelPitchW:pixelPitchW,pixelPitchH:pixelPitchH,coveragePct:coveragePct},nodeLoads:nodeLoads,processorLoads:processorLoads,cablePlan:cablePlan,issues:issues}}function resolveProcessorCapacity(processor,metadata){const latencyMode="From Project"===processor.latencyMode?metadata.latencyMode:processor.latencyMode;if("custom"===processor.capacityMode)return{capacityMPix:clampFloat(processor.customMPix,.1,1e3),matchedFps:nearestKnownFps(metadata.frameRate),source:"Custom MPix",latencyMode:latencyMode};const reg=PROCESSOR_REGISTRY[processor.profile];if(reg){const fps=reg.nearestFps(metadata.frameRate),bpc=toBpc(metadata.colorDepth);return{capacityMPix:reg.capacityLookup(fps,bpc,latencyMode),matchedFps:fps,source:`${processor.profile} table`,latencyMode:latencyMode}}return{capacityMPix:clampFloat(processor.customMPix,.1,1e3),matchedFps:nearestKnownFps(metadata.frameRate),source:"Custom profile fallback",latencyMode:latencyMode}}function resolveXdPortCapacity(metadata,latencyModeResolved){const fps=nearestKnownFps(metadata.frameRate),row=XD_PORT_TABLE[fps]||XD_PORT_TABLE[24],mode="ULL"===latencyModeResolved?"ull":"standard",bpc=toBpc(metadata.colorDepth);return{portPixels:row[mode][bpc]??row[mode][10]??row[mode][8],matchedFps:fps,bpc:bpc}}function nearestHeliosFps(raw){const n=Number.parseFloat(raw);if(!Number.isFinite(n))return 24;for(const key of HELIOS_FPS_KEYS)if(key>=n)return key;return HELIOS_FPS_KEYS[HELIOS_FPS_KEYS.length-1]}function resolvePortCapacityForProcessor(processor,metadata){const latencyMode="From Project"===processor.latencyMode?metadata.latencyMode:processor.latencyMode,reg=PROCESSOR_REGISTRY[processor.profile];if(reg){const fps=reg.nearestFps(metadata.frameRate),bpc=toBpc(metadata.colorDepth);return{portPixels:reg.portLookup(fps,bpc,latencyMode),matchedFps:fps,bpc:bpc,portUnitLabel:reg.portUnitLabel,portsPerUnit:reg.portsPerUnit}}return{...resolveXdPortCapacity(metadata,latencyMode),portUnitLabel:"XD",portsPerUnit:10}}function nearestKnownFps(raw){const n=Number.parseFloat(raw);if(!Number.isFinite(n))return 24;let best=PERF_FPS_KEYS[0],delta=Math.abs(n-best);for(const key of PERF_FPS_KEYS){const d=Math.abs(n-key);d<delta&&(best=key,delta=d)}return best}function toBpc(depth){return String(depth).startsWith("8")?8:String(depth).startsWith("12")?12:10}function processorModeLabel(processor){return`${"custom"===processor.capacityMode?`Custom ${processor.customMPix.toFixed(2)} MPix`:processor.profile} / ${processor.latencyMode}`}function deploymentSummary(report){const lines=[];lines.push("# LED WALL DEPLOYMENT"),lines.push(`Project: ${state.metadata.projectName}`),lines.push(`Build: ${state.metadata.buildTag}`),lines.push(`Video: ${state.metadata.frameRate}fps ${state.metadata.colorSpace} ${state.metadata.colorDepth} ${state.metadata.eotf}`),lines.push(`Sync: ${state.metadata.syncMode} (${state.metadata.genlockSource})`),lines.push(`Latency: ${state.metadata.latencyMode}`),lines.push(`Redundancy: ${state.metadata.redundancy}`),lines.push(`Control Net: ${state.metadata.controlNetwork}`),lines.push(""),lines.push(`Wall: ${report.wall.wallPxW}x${report.wall.wallPxH} (${state.wall.wallTilesX}x${state.wall.wallTilesY} cabinets)`),lines.push(`Coverage: ${report.wall.coveragePct.toFixed(1)}%`),lines.push(""),lines.push("Render Nodes:"),Object.values(report.nodeLoads).forEach(load=>{const used=load.totalPixels/1e6;lines.push(`- ${load.node.name}: ${used.toFixed(2)} / ${load.node.maxMPix.toFixed(2)} MPix`),load.regions.forEach(r=>{const pxW=r.w*state.wall.tilePxW,pxH=r.h*state.wall.tilePxH;lines.push(`  • ${r.name} ${r.x},${r.y} ${r.w}x${r.h} tiles (${pxW}x${pxH}px) ${r.role}`)})}),lines.push(""),lines.push("Processors:");let totalXds=0;return Object.values(report.processorLoads).forEach(load=>{const used=load.totalPixels/1e6;lines.push(`- ${load.processor.name} (${load.processor.type}): ${used.toFixed(2)} / ${load.capacityMPix.toFixed(2)} MPix`),lines.push(`  source=${load.source} fps=${load.matchedFps} latency=${load.latencyMode}`),lines.push(`  ${load.portUnitLabel||"XD"} per-port=${formatInt(load.xdPortCapacityPixels)} px, ports=${load.xdPortsRequired}, units=${load.xdsRequired}`),totalXds+=load.xdsRequired}),lines.push(`Total XD units required (sum across processors): ${totalXds}`),lines.push(""),lines.push("Validation:"),report.issues.forEach(i=>lines.push(`- [${i.level.toUpperCase()}] ${i.message}`)),lines.join("\n")}function showToast(message,duration=2400){let container=document.getElementById("toast-container");container||(container=document.createElement("div"),container.id="toast-container",document.body.appendChild(container));const toast=document.createElement("div");toast.className="toast-msg",toast.textContent=message,container.appendChild(toast),requestAnimationFrame(()=>{toast.classList.add("toast-visible")}),setTimeout(()=>{toast.classList.remove("toast-visible"),setTimeout(()=>toast.remove(),300)},duration)}function showDisclaimer(){const modal=document.createElement("div");modal.className="po-modal-backdrop",modal.innerHTML='\n    <div class="po-modal disclaimer-modal">\n      <div class="po-header">\n        <h2>Disclaimer</h2>\n      </div>\n      <div class="po-body disclaimer-body">\n        <p>This tool uses its own calculation methods to estimate the number of processors, XD units, and other hardware required for your LED wall configuration.</p>\n        <p><strong>These estimates may not be fully accurate.</strong> Actual hardware requirements can vary based on firmware versions, signal routing, redundancy configurations, and other factors not fully modelled here.</p>\n        <p>Please always refer to the original equipment manufacturers (OEMs) - such as Brompton Technology and Megapixel VR - for verified and up-to-date specifications before finalising your setup.</p>\n        <p>This tool is intended for <strong style="color:#ff4f63">rough planning and estimation purposes only</strong>.</p>\n        <button class="tool-btn primary disclaimer-accept-btn">I Understand</button>\n      </div>\n    </div>',document.body.appendChild(modal),modal.querySelector(".disclaimer-accept-btn").addEventListener("click",()=>modal.remove())}function showHowTo(){const modal=document.createElement("div");modal.className="po-modal-backdrop",modal.innerHTML='\n    <div class="po-modal howto-modal">\n      <div class="po-header">\n        <h2>How to Use</h2>\n        <button class="tool-btn po-close howto-close-btn" title="Close"><span class="icon-mask btn-icon icon-close" style="margin-right:0;"></span></button>\n      </div>\n      <div class="po-body howto-body">\n        <h3>Getting Started</h3>\n        <ul>\n          <li>Set your <strong>Video Settings</strong> (FPS, latency, bit depth) and <strong>Wall Settings</strong> (tile count, cabinet size) in the top toolbar.</li>\n          <li>Use <strong>Outliner</strong> (left panel) to add Render Nodes and Processors, then add Render Regions to each node.</li>\n          <li>Click a region in the canvas or outliner to select it. Use <strong>Details</strong> (right panel) to assign it to a node and processor.</li>\n        </ul>\n        <h3>Canvas Controls</h3>\n        <ul>\n          <li><strong>Drag region</strong> to move it. <strong>Drag corner handles</strong> to resize.</li>\n          <li><strong>Right-click drag</strong> or <strong>Alt + drag</strong> or <strong>drag empty area</strong> to pan the view.</li>\n          <li><strong>Scroll wheel</strong> to zoom in/out.</li>\n          <li><strong>Delete key</strong> removes the selected region.</li>\n          <li><strong>Enter key</strong> closes the details panel.</li>\n          <li>Press <strong>Fit (<span class="icon-mask btn-icon icon-fit" style="margin-right:0;"></span>)</strong> to reset the view.</li>\n        </ul>\n        <h3>Planning Tools</h3>\n        <ul>\n          <li><strong>Auto Pack</strong>: Automatically creates the optimal number of processors, nodes, and regions based on your current settings.</li>\n          <li><strong>Suggest Configs</strong>: Lists feasible hardware configurations.</li>\n          <li><strong>Processor Overview</strong>: Table of per-processor load, XD usage, and region breakdown.</li>\n          <li><strong>Validate</strong>: Checks for overlaps, capacity violations, and unassigned regions.</li>\n          <li>Toggle <strong>cable plan overlay (<span class="icon-mask btn-icon icon-cable" style="margin-right:0;"></span>)</strong> to visualise XD port daisy-chain routing.</li>\n        </ul>\n        <h3>Unassigned Regions</h3>\n        <ul>\n          <li>Regions without a node assignment appear with <strong>grey diagonal hatching</strong> on the canvas.</li>\n          <li>Unassigned node/processor dropdowns are highlighted in <strong>red</strong> in the details panel.</li>\n          <li>Each render node supports a maximum of <strong>4 regions</strong>.</li>\n        </ul>\n        <button class="tool-btn primary howto-close-btn" style="margin-top:8px;">Got it</button>\n      </div>\n    </div>',document.body.appendChild(modal),modal.querySelectorAll(".howto-close-btn").forEach(btn=>btn.addEventListener("click",()=>modal.remove())),modal.addEventListener("click",e=>{e.target===modal&&modal.remove()})}function showHistoryModal(){loadHistoryStore();const existing=document.getElementById("historyModal");existing&&existing.remove();const entries=getHistoryEntriesNewestFirst(),modal=document.createElement("div");modal.id="historyModal",modal.className="po-modal-backdrop",modal.innerHTML=`\n    <div class="po-modal history-modal">\n      <div class="po-header">\n        <h2>History</h2>\n        <span class="po-sub">${entries.length} snapshot(s) saved</span>\n        <button class="tool-btn po-close" title="Close">✕</button>\n      </div>\n      <div class="po-body history-body">\n        <div class="history-toolbar">\n          <button class="tool-btn tiny" data-history-action="export-csv" ${entries.length?"":"disabled"}>Export CSV</button>\n          <button class="tool-btn tiny" data-history-action="clear" ${entries.length?"":"disabled"}>Clear History</button>\n        </div>\n        ${entries.length?`\n          <div class="history-list">\n            ${entries.map(entry=>`\n              <div class="history-item">\n                <div class="history-item-main">\n                  <div class="history-item-title">${escapeHtml(entry.message||"Snapshot")}</div>\n                  <div class="history-item-meta">${renderHistoryEntryMeta(entry)}</div>\n                </div>\n                <div class="history-item-actions">\n                  <button class="tool-btn tiny" data-history-action="restore" data-history-id="${entry.id}">Restore</button>\n                  <button class="tool-btn tiny danger" data-history-action="delete" data-history-id="${entry.id}">Delete</button>\n                </div>\n              </div>`).join("")}\n          </div>`:'<div class="history-empty">No snapshots yet. Changes will appear here automatically.</div>'}\n      </div>\n    </div>`,document.body.appendChild(modal),modal.querySelector(".po-close")?.addEventListener("click",()=>modal.remove()),modal.addEventListener("click",e=>{if(e.target===modal)return void modal.remove();const btn=e.target.closest("[data-history-action]");if(!btn)return;const action=btn.dataset.historyAction,id=btn.dataset.historyId;return"restore"===action&&id?(restoreHistoryEntry(id),void modal.remove()):"delete"===action&&id?(deleteHistoryEntry(id),modal.remove(),void showHistoryModal()):void("export-csv"!==action?"clear"===action&&(clearHistoryEntries(),modal.remove(),showHistoryModal()):exportEntriesCsv(entries,"history"))})}function showProcessorOverview(){const report=calculateReport(),tilePixels=Math.max(1,state.wall.tilePxW*state.wall.tilePxH),settingsLabel=`${state.metadata.frameRate} fps | ${state.metadata.colorDepth} | ${state.metadata.latencyMode} | Cabinet ${state.wall.tilePxW}x${state.wall.tilePxH} px`,csvHeader=["Processor","Resolution","Load (MPix)","Units / Ports","Unit Breakdown","Regions"],csvRows=[];let tableRows="",totalXdUnits=0;state.processors.forEach(processor=>{const load=report.processorLoads[processor.id]||{totalPixels:0,capacityMPix:0,xdPortCapacityPixels:0,xdPortsRequired:0,xdsRequired:0},regions=state.regions.filter(r=>r.processorId===processor.id),usedPixels=load.totalPixels||0,usedMPix=usedPixels/1e6,util=load.capacityMPix>0?usedMPix/load.capacityMPix*100:0,totalTiles=regions.reduce((acc,r)=>acc+r.w*r.h,0),xds=Math.max(0,load.xdsRequired||0),unitLabel=load.portUnitLabel||"XD",ppu=load.portsPerUnit||10;totalXdUnits+=xds;const effectiveRes=1===regions.length?`${regions[0].w*state.wall.tilePxW}x${regions[0].h*state.wall.tilePxH}`:regions.length>1?`${formatInt(usedPixels)} px (${regions.length} regions)`:"N/A",utilClass=util>95?"po-warn":util>80?"po-ok":"",portWarn=(load.portsPerProcessorLimit||0)>0&&load.xdPortsRequired>load.portsPerProcessorLimit;let xdDetail="";if(xds>0){const baseTiles=Math.floor(totalTiles/xds),tileRemainder=totalTiles%xds,unitCapPixels=ppu*Math.max(1,load.xdPortCapacityPixels||0),xdParts=[];for(let xi=0;xi<xds;xi++){const panels=baseTiles+(xi<tileRemainder?1:0),utilUnit=unitCapPixels>0?panels*tilePixels/unitCapPixels*100:0;xdParts.push(`${unitLabel}${xi+1}: ${panels}p (${utilUnit.toFixed(0)}%)`)}xdDetail=xdParts.join(", ")}const regionDetailCsv=regions.map(r=>`${r.name}: ${r.w}x${r.h}`).join(", ")||"N/A",regionDetail=escapeHtml(regionDetailCsv),loadDetail=`${usedMPix.toFixed(2)} / ${load.capacityMPix.toFixed(2)} (${util.toFixed(0)}%)`,unitsPortsDetail=`${load.xdPortsRequired} ports / ${xds} ${unitLabel}${portWarn?" ⚠":""}`;tableRows+=`<tr>\n      <td>${escapeHtml(processor.name)}</td>\n      <td>${effectiveRes}</td>\n      <td class="${utilClass}">${loadDetail}</td>\n      <td${portWarn?' class="po-warn"':""}>${unitsPortsDetail}</td>\n      <td>${xdDetail||"N/A"}</td>\n      <td>${regionDetail}</td>\n    </tr>`,csvRows.push([processor.name,effectiveRes,loadDetail,unitsPortsDetail,xdDetail||"N/A",regionDetailCsv])});const existing=document.getElementById("processorOverviewModal");existing&&existing.remove();const modal=document.createElement("div");modal.id="processorOverviewModal",modal.className="po-modal-backdrop",modal.innerHTML=`\n    <div class="po-modal">\n      <div class="po-header">\n        <h2>Processor Overview</h2>\n        <span class="po-sub">${escapeHtml(settingsLabel)} | ${state.processors.length} proc, ${totalXdUnits} port unit(s) total</span>\n        <button class="tool-btn tiny po-copy-csv" title="Copy processor overview as CSV">Copy CSV</button>\n        <button class="tool-btn po-close" title="Close">✕</button>\n      </div>\n      <div class="po-body">\n        <table class="po-table">\n          <thead><tr>\n            <th>Processor</th>\n            <th>Resolution</th>\n            <th>Load (MPix)</th>\n            <th>Units / Ports</th>\n            <th>Unit Breakdown</th>\n            <th>Regions</th>\n          </tr></thead>\n          <tbody>${tableRows}</tbody>\n        </table>\n      </div>\n    </div>`,document.body.appendChild(modal),modal.querySelector(".po-copy-csv")?.addEventListener("click",async()=>{const csv=[csvHeader,...csvRows].map(row=>row.map(csvEscape).join(",")).join("\n");showToast(await copyTextToClipboard(csv)?"Processor overview copied as CSV":"Could not copy CSV")}),modal.querySelector(".po-close").addEventListener("click",()=>modal.remove()),modal.addEventListener("click",e=>{e.target===modal&&modal.remove()})}function getPortsPerProcessorLimit(processor){if(!processor)return 9999;const reg=PROCESSOR_REGISTRY[processor.profile];if(!reg||reg.maxUnits===1/0)return 9999;const base=reg.portsPerUnit*reg.maxUnits;return"Full Mirror"===state.metadata.redundancy?Math.max(1,Math.floor(base/2)):base}function toggleCablePlanOverlay(){if(view.showCableOverlay)return view.showCableOverlay=!1,syncCanvasMetaButtons(),renderCanvas(calculateReport()),void renderLog("Cable plan overlay disabled");const report=calculateReport(),lines=formatCablePlan(report.cablePlan);view.showCableOverlay=!0,syncCanvasMetaButtons(),appendLogBlock("Cable plan generated (overlay enabled)",lines),setDrawerOpen(els.logDrawer,!0),syncDrawerButtons(),renderCanvas(report)}function exportConfig(){const payload={metadata:state.metadata,wall:state.wall,nodes:state.nodes,processors:state.processors,regions:state.regions,summary:deploymentSummary(calculateReport())},blob=new Blob([JSON.stringify(payload,null,2)],{type:"application/json"}),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url,a.download=`${slug(state.metadata.projectName||"led-wall")}-config.json`,document.body.appendChild(a),a.click(),a.remove(),URL.revokeObjectURL(url),renderLog("Config exported")}function exportSvg(){const W=state.wall.wallTilesX,H=state.wall.wallTilesY,wallW=40*W,wallH=40*H,svgW=wallW+48,svgH=wallH+48+32,report=calculateReport(),parts=[];parts.push(`<svg xmlns="http://www.w3.org/2000/svg" width="${svgW}" height="${svgH}" viewBox="0 0 ${svgW} ${svgH}" font-family="'Roboto Mono',monospace">`),parts.push('<defs>\n  <pattern id="hatch" width="8" height="8" patternUnits="userSpaceOnUse">\n    <line x1="0" y1="0" x2="8" y2="8" stroke="rgba(160,160,180,0.35)" stroke-width="1"/>\n  </pattern>\n</defs>'),parts.push(`<rect width="${svgW}" height="${svgH}" fill="#0a0b10"/>`),parts.push(`<rect x="24" y="24" width="${wallW}" height="${wallH}" fill="#0b1525"/>`);for(let x=1;x<W;x++){const px=24+40*x;parts.push(`<line x1="${px}" y1="24" x2="${px}" y2="${24+wallH}" stroke="rgba(143,175,228,0.22)" stroke-width="1"/>`)}for(let y=1;y<H;y++){const py=24+40*y;parts.push(`<line x1="24" y1="${py}" x2="${24+wallW}" y2="${py}" stroke="rgba(143,175,228,0.22)" stroke-width="1"/>`)}state.regions.forEach((region,idx)=>{const unassigned=!region.nodeId,c=unassigned?"#888899":colorForRegion(region,idx),rx=24+40*region.x,ry=24+40*region.y,rw=40*region.w,rh=40*region.h,label=escapeHtml(`${region.name}  ${region.w*state.wall.tilePxW}x${region.h*state.wall.tilePxH}px`),textFill=unassigned?"#aaaacc":"#edf4ff";parts.push("<g>"),parts.push(`<rect x="${rx}" y="${ry}" width="${rw}" height="${rh}" fill="${hexToRgba(c,.18)}" stroke="${c}" stroke-width="1.4"/>`),unassigned&&parts.push(`<rect x="${rx}" y="${ry}" width="${rw}" height="${rh}" fill="url(#hatch)"/>`);const clipId=`cl${idx}`;parts.push(`<clipPath id="${clipId}"><rect x="${rx}" y="${ry}" width="${rw}" height="${rh}"/></clipPath>`),parts.push(`<text x="${rx+5}" y="${ry+14}" fill="${textFill}" font-size="11" clip-path="url(#${clipId})">${label}</text>`),parts.push("</g>")}),parts.push(`<rect x="24" y="24" width="${wallW}" height="${wallH}" fill="none" stroke="#6b8fca" stroke-width="1.5"/>`);const{wallPxW:wallPxW,wallPxH:wallPxH,wallMmW:wallMmW,wallMmH:wallMmH}=report.wall,dimLabel=escapeHtml(`${wallPxW}x${wallPxH}px  |  ${(wallMmW/1e3).toFixed(2)}m x ${(wallMmH/1e3).toFixed(2)}m`),lx=24+wallW/2,ly=24+wallH+20;parts.push(`<rect x="24" y="${ly-14}" width="${wallW}" height="20" fill="rgba(10,12,20,0.65)"/>`),parts.push(`<text x="${lx}" y="${ly}" text-anchor="middle" fill="#9eb8e8" font-size="11" font-weight="bold">${dimLabel}</text>`),parts.push("</svg>");const blob=new Blob([parts.join("\n")],{type:"image/svg+xml"}),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url,a.download=`${slug(state.metadata.projectName||"led-wall")}-layout.svg`,document.body.appendChild(a),a.click(),a.remove(),URL.revokeObjectURL(url),renderLog("SVG layout exported")}function exportCableSvg(){const W=state.wall.wallTilesX,H=state.wall.wallTilesY,wallW=40*W,wallH=40*H,svgW=wallW+48,svgH=wallH+48+32,report=calculateReport(),cablePlan=report.cablePlan,parts=[];parts.push(`<svg xmlns="http://www.w3.org/2000/svg" width="${svgW}" height="${svgH}" viewBox="0 0 ${svgW} ${svgH}" font-family="'Roboto Mono',monospace">`),parts.push('<defs>\n  <pattern id="hatch" width="8" height="8" patternUnits="userSpaceOnUse">\n    <line x1="0" y1="0" x2="8" y2="8" stroke="rgba(160,160,180,0.35)" stroke-width="1"/>\n  </pattern>\n</defs>'),parts.push(`<rect width="${svgW}" height="${svgH}" fill="#0a0b10"/>`),parts.push(`<rect x="24" y="24" width="${wallW}" height="${wallH}" fill="#0b1525"/>`);for(let x=1;x<W;x++){const px=24+40*x;parts.push(`<line x1="${px}" y1="24" x2="${px}" y2="${24+wallH}" stroke="rgba(143,175,228,0.15)" stroke-width="1"/>`)}for(let y=1;y<H;y++){const py=24+40*y;parts.push(`<line x1="24" y1="${py}" x2="${24+wallW}" y2="${py}" stroke="rgba(143,175,228,0.15)" stroke-width="1"/>`)}if(state.regions.forEach((region,idx)=>{const unassigned=!region.nodeId,c=unassigned?"#888899":colorForRegion(region,idx),rx=24+40*region.x,ry=24+40*region.y,rw=40*region.w,rh=40*region.h;parts.push(`<rect x="${rx}" y="${ry}" width="${rw}" height="${rh}" fill="none" stroke="${c}" stroke-width="1" opacity="0.4"/>`),unassigned&&parts.push(`<rect x="${rx}" y="${ry}" width="${rw}" height="${rh}" fill="url(#hatch)"/>`);const clipId=`cr${idx}`,nameLabel=escapeHtml(region.name);parts.push(`<clipPath id="${clipId}"><rect x="${rx}" y="${ry}" width="${rw}" height="${rh}"/></clipPath>`),parts.push(`<text x="${rx+4}" y="${ry+13}" fill="${c}" font-size="10" opacity="0.6" clip-path="url(#${clipId})">${nameLabel}</text>`)}),cablePlan?.processorPlans?.length){let globalXdOffset=0;cablePlan.processorPlans.forEach(procPlan=>{procPlan.xds.forEach(xd=>{const displayXdIdx=view.globalUnitNumbering?globalXdOffset+xd.index:xd.index;xd.ports.forEach(port=>{if(!port.segments?.length)return;const tilePath=buildPortTilePath(port),stepCount=tilePath.length;if(!stepCount)return;tilePath.forEach((tile,idx)=>{const fill=cableFillColorForStep(tile.base,port.index,idx,stepCount,.72),tx=24+40*tile.x+1,ty=24+40*tile.y+1;parts.push(`<rect x="${tx}" y="${ty}" width="38" height="38" rx="2" fill="${fill}"/>`)});const first=tilePath[0],lx=24+40*first.x+3,ly=24+40*first.y+12,portLabel=escapeHtml(`${procPlan.portUnitPrefix||"X"}${displayXdIdx}P${port.index}`);parts.push(`<text x="${lx}" y="${ly}" fill="rgba(255,255,255,0.9)" font-size="9" font-weight="bold">${portLabel}</text>`);for(let i=0;i<stepCount-1;i++){const stroke=cableArrowColorForStep(tilePath[i].base,port.index,i,stepCount-1,.9),cx1=24+40*(tilePath[i].x+.5),cy1=24+40*(tilePath[i].y+.5),cx2=24+40*(tilePath[i+1].x+.5),cy2=24+40*(tilePath[i+1].y+.5),dx=cx2-cx1,dy=cy2-cy1,len=Math.hypot(dx,dy);if(len<1)continue;const ux=dx/len,uy=dy/len,pad=6,sx=cx1+ux*pad,sy=cy1+uy*pad,ex=cx2-ux*pad,ey=cy2-uy*pad;parts.push(`<line x1="${sx.toFixed(1)}" y1="${sy.toFixed(1)}" x2="${ex.toFixed(1)}" y2="${ey.toFixed(1)}" stroke="${stroke}" stroke-width="2" stroke-linecap="round"/>`);const head=7,angle=Math.atan2(ey-sy,ex-sx),ax1=ex-head*Math.cos(angle-Math.PI/7),ay1=ey-head*Math.sin(angle-Math.PI/7),ax2=ex-head*Math.cos(angle+Math.PI/7),ay2=ey-head*Math.sin(angle+Math.PI/7);parts.push(`<polygon points="${ex.toFixed(1)},${ey.toFixed(1)} ${ax1.toFixed(1)},${ay1.toFixed(1)} ${ax2.toFixed(1)},${ay2.toFixed(1)}" fill="${stroke}"/>`)}})}),globalXdOffset+=procPlan.xds.length})}parts.push(`<rect x="24" y="24" width="${wallW}" height="${wallH}" fill="none" stroke="#6b8fca" stroke-width="1.5"/>`);const{wallPxW:wallPxW,wallPxH:wallPxH,wallMmW:wallMmW,wallMmH:wallMmH}=report.wall,patternName={linear:"Linear",S:"S-Shape",invS:"Inv. S",N:"N-Shape",invN:"Inv. N"}[state.metadata.wiringPattern||"linear"]||"Linear",dimLabel=escapeHtml(`${wallPxW}x${wallPxH}px  |  ${(wallMmW/1e3).toFixed(2)}m x ${(wallMmH/1e3).toFixed(2)}m  |  ${patternName} wiring`),lx=24+wallW/2,ly=24+wallH+20;parts.push(`<rect x="24" y="${ly-14}" width="${wallW}" height="20" fill="rgba(10,12,20,0.65)"/>`),parts.push(`<text x="${lx}" y="${ly}" text-anchor="middle" fill="#9eb8e8" font-size="11" font-weight="bold">${dimLabel}</text>`),parts.push("</svg>");const blob=new Blob([parts.join("\n")],{type:"image/svg+xml"}),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url,a.download=`${slug(state.metadata.projectName||"led-wall")}-wiring.svg`,document.body.appendChild(a),a.click(),a.remove(),URL.revokeObjectURL(url),renderLog("Wiring SVG exported")}async function importConfig(e){const file=e.target.files?.[0];if(file)try{const json=JSON.parse(await file.text());if(state.metadata={...state.metadata,...json.metadata||{}},!state.metadata.processorProfile&&json.processors?.length){const firstProfile=json.processors[0]?.profile;firstProfile&&PROCESSOR_PROFILES.includes(firstProfile)&&(state.metadata.processorProfile=firstProfile)}state.wall={...state.wall,...json.wall||{}},sanitizeWall(),state.nodes=ensureArray(json.nodes).map((n,i)=>({id:n.id||uid("node"),name:n.name||`RN-${i+1}`,maxWidth:clampInt(n.maxWidth,1,3e4),maxHeight:clampInt(n.maxHeight,1,3e4),maxMPix:clampFloat(n.maxMPix,.1,1e3),outputs:clampInt(n.outputs,1,16),backupNodeId:n.backupNodeId||""})),state.processors=ensureArray(json.processors).map((p,i)=>{const processor={id:p.id||uid("proc"),name:p.name||`PROC-${i+1}`,type:p.type||"Custom Processor",profile:p.profile||"Custom",capacityMode:p.capacityMode||"custom",latencyMode:p.latencyMode||"From Project",customMPix:clampFloat(p.customMPix??p.maxMPix??9,.1,1e3)};return sanitizeProcessor(processor),processor}),state.regions=ensureArray(json.regions).map((r,i)=>({id:r.id||uid("reg"),name:r.name||`Region ${i+1}`,x:clampInt(r.x,0,1e4),y:clampInt(r.y,0,1e4),w:clampInt(r.w,1,1e4),h:clampInt(r.h,1,1e4),nodeId:r.nodeId||"",processorId:r.processorId||"",role:r.role||"Background"})),state.nodes.length||state.nodes.push({id:uid("node"),name:"RN-01",maxWidth:7680,maxHeight:4320,maxMPix:14,outputs:2,backupNodeId:""}),state.processors.length||state.processors.push(defaultProcessor("PROC-A")),state.regions.length||state.regions.push({id:uid("reg"),name:"Main",x:0,y:0,w:12,h:8,nodeId:state.nodes[0].id,processorId:state.processors[0].id,role:"Frustum"}),syncProcessorProfiles(),state.selection={type:"region",id:state.regions[0].id},fitView(),renderAll("Config imported")}catch{renderLog("Import failed: invalid JSON")}finally{e.target.value=""}}function removeSelected(){if(state.selection.id){if("region"===state.selection.type){const before=state.regions.length;if(state.regions=state.regions.filter(r=>r.id!==state.selection.id),state.regions.length<before)return state.selection={type:"none",id:""},void renderAll("Render area removed")}if("node"===state.selection.type){const id=state.selection.id,node=state.nodes.find(n=>n.id===id);return state.nodes=state.nodes.filter(n=>n.id!==id),state.regions.forEach(r=>{r.nodeId===id&&(r.nodeId="")}),state.selection={type:"none",id:""},void renderAll("Render node removed"+(node?` (${node.name})`:""))}if("processor"===state.selection.type){const id=state.selection.id,processor=state.processors.find(p=>p.id===id);return state.processors=state.processors.filter(p=>p.id!==id),state.regions.forEach(r=>{r.processorId===id&&(r.processorId="")}),state.selection={type:"none",id:""},void renderAll("Processor removed"+(processor?` (${processor.name})`:""))}renderLog("Selected item type cannot be removed here")}else renderLog("Nothing selected to remove")}function select(type,id){state.selection={type:type,id:id}}function isSelected(type,id){return state.selection.type===type&&state.selection.id===id}function getSelectedObject(forceType){const type=forceType||state.selection.type;return state.selection.id?"node"===type?state.nodes.find(n=>n.id===state.selection.id)||null:"processor"===type?state.processors.find(p=>p.id===state.selection.id)||null:"region"===type&&state.regions.find(r=>r.id===state.selection.id)||null:null}function renderSelectionBadge(){const sel=getSelectedObject();if(sel){if("node"===state.selection.type)els.selectionBadge.textContent=`Node: ${sel.name}`;else if("processor"===state.selection.type){const resolved=resolveProcessorCapacity(sel,state.metadata);els.selectionBadge.textContent=`Processor: ${sel.name} (${resolved.capacityMPix.toFixed(2)} MPix)`}else if("region"===state.selection.type){const pxW=sel.w*state.wall.tilePxW,pxH=sel.h*state.wall.tilePxH;els.selectionBadge.textContent=`Area: ${sel.name} (${pxW}x${pxH}px)`}}else els.selectionBadge.textContent="No selection"}function renderLog(message){const line=`${(new Date).toLocaleTimeString()}  ${message}`;state.logLines.push(line),state.logLines.length>160&&state.logLines.shift(),els.outputLog.textContent=state.logLines.join("\n"),els.outputLog.scrollTop=els.outputLog.scrollHeight}function appendLogBlock(header,lines){const stamp=`${(new Date).toLocaleTimeString()}  ${header}`;state.logLines.push(stamp);for(const line of lines)state.logLines.push(`          ${line}`);state.logLines.length>320&&(state.logLines=state.logLines.slice(-320)),els.outputLog.textContent=state.logLines.join("\n"),els.outputLog.scrollTop=els.outputLog.scrollHeight}function suggestConfigs(){const hw=calculateOptimalHardware();hw.feasible?(showSuggestConfigsModal(hw),renderLog(`Suggest Configs: best is ${hw.best.processors} ${hw.settings.procLabel}(s), ${hw.best.totalXds} ${hw.settings.unitLabel}(s).`)):renderLog(`Suggest Configs failed: ${hw.error||"no feasible configuration found."}`)}function showSuggestConfigsModal(hw){const existing=document.getElementById("suggestConfigsModal");existing&&existing.remove();const{wallPxW:wallPxW,wallPxH:wallPxH,nodeMaxW:nodeMaxW,nodeMaxH:nodeMaxH}=hw.wallInfo,{tilePxW:tilePxW,tilePxH:tilePxH}=state.wall,settingsLabel=`${hw.settings.fps} Hz · ${hw.settings.latency} · ${hw.settings.depth}`,portraitBest=hw.allUniformCandidates.filter(c=>"portrait"===c.orientation).sort((a,b)=>a.processors-b.processors||a.totalXds-b.totalXds)[0]||null,landscapeBest=hw.allUniformCandidates.filter(c=>"portrait"!==c.orientation).sort((a,b)=>a.processors-b.processors||a.totalXds-b.totalXds)[0]||null,mixedBest=hw.bandCandidate||null,allThree=[portraitBest,landscapeBest,mixedBest].filter(Boolean),minSX40=allThree.length?Math.min(...allThree.map(c=>c.processors)):1/0;function specRow(label,value){return`<div class="sc-spec-row"><span class="sc-spec-key">${label}</span><span class="sc-spec-val">${escapeHtml(String(value))}</span></div>`}function buildCard(mode,title,subtitle,candidate,blockReason){const isOptimal=candidate&&candidate.processors===minSX40,cardClass=`sc-card sc-card--${mode}${isOptimal?" sc-best":""}${candidate?"":" sc-blocked"}`,badge=isOptimal?'<span class="sc-best-badge">★ Optimal</span>':"";if(!candidate){return`<div class="${cardClass}">\n        <div class="sc-card-header">\n          <span class="sc-mode-label">${title}</span>${badge}\n        </div>\n        <div class="sc-card-body">\n          <div class="sc-subtitle">${subtitle}</div>\n          <div class="sc-unavailable"><strong>Not feasible</strong>${escapeHtml(blockReason||"No valid splits for this wall tile dimensions")}</div>\n        </div>\n      </div>`}const isBand="band"===candidate.type,mpix=isBand?((candidate.allRegions?.[0]?.pixels??0)/1e6).toFixed(2):(candidate.segPxW*candidate.segPxH/1e6).toFixed(2),util=isBand?`${(candidate.avgUtil??0).toFixed(0)}%`:`${(candidate.utilization??0).toFixed(0)}%`,layout=isBand?`${candidate.bands?.length??"?"} rows (non-uniform)`:`${candidate.cols} col${candidate.cols>1?"s":""} × ${candidate.rows} row${candidate.rows>1?"s":""}`,regionSize=isBand?(()=>{const sizes=(candidate.allRegions??[]).map(r=>`${r.w*tilePxW}×${r.h*tilePxH}`),unique=[...new Set(sizes)];return unique.length<=2?unique.join(", ")+"px":`${unique.length} sizes`})():`${candidate.segPxW}×${candidate.segPxH}px`,pu=hw.settings.parentUnit;let displayXds=candidate.totalXds,displayQds=0;return pu&&(displayQds=Math.max(candidate.processors,Math.ceil(candidate.totalXds/pu.unitsPerParent)),displayXds=Math.max(displayQds,candidate.totalXds)),`<div class="${cardClass}">\n      <div class="sc-card-header">\n        <span class="sc-mode-label">${title}</span>${badge}\n      </div>\n      <div class="sc-card-body">\n        <div class="sc-subtitle">${subtitle}</div>\n        <div class="sc-metrics">\n          <div class="sc-metric-item">\n            <div class="sc-metric-value">${candidate.processors}</div>\n            <div class="sc-metric-key">${hw.settings.procLabel}</div>\n          </div>\n          <div class="sc-metric-sep"></div>\n          <div class="sc-metric-item">\n            <div class="sc-metric-value">${pu?displayQds:displayXds}</div>\n            <div class="sc-metric-key">${pu?pu.label:hw.settings.unitLabel+" Units"}</div>\n          </div>\n          <div class="sc-metric-sep"></div>\n          <div class="sc-metric-item">\n            <div class="sc-metric-value">${mpix}</div>\n            <div class="sc-metric-key">MPix/rgn</div>\n          </div>\n        </div>\n        <div class="sc-specs">\n          ${specRow("Layout",layout)}\n          ${specRow("Region size",regionSize)}\n          ${specRow(`${hw.settings.procLabel} util`,util)}\n          ${pu?specRow(`${pu.label} required`,displayQds):""}\n          ${pu?specRow(`${hw.settings.unitLabel} required`,displayXds):""}\n        </div>\n        <button class="sc-apply-btn" data-apply-key="${title}">Apply this config</button>\n      </div>\n    </div>`}const modal=document.createElement("div");modal.id="suggestConfigsModal",modal.className="po-modal-backdrop",modal.innerHTML=`\n    <div class="po-modal sc-modal">\n      <div class="po-header">\n        <h2>Suggested Configurations</h2>\n        <span class="po-sub">Wall ${wallPxW}×${wallPxH}px · Node max ${nodeMaxW}×${nodeMaxH}px · ${settingsLabel}</span>\n        <button class="tool-btn po-close" title="Close">✕</button>\n      </div>\n      <div class="sc-cards">\n        ${buildCard("portrait","Portrait","Node rotated 90° - tall narrow strips",portraitBest,hw.portraitBlockReason)}\n        ${buildCard("landscape","Landscape","Node normal orientation - wide short regions",landscapeBest,hw.landscapeBlockReason)}\n        ${buildCard("mixed","Mixed","Non-uniform bands - mixed region widths per row",mixedBest,null)}\n      </div>\n    </div>`,document.body.appendChild(modal),modal.querySelector(".po-close").addEventListener("click",()=>modal.remove()),modal.addEventListener("click",e=>{e.target===modal&&modal.remove()});const candidateMap={Portrait:portraitBest,Landscape:landscapeBest,Mixed:mixedBest};modal.querySelectorAll(".sc-apply-btn").forEach(btn=>{btn.addEventListener("click",()=>{const candidate=candidateMap[btn.dataset.applyKey];candidate&&(modal.remove(),applyHardwareRecommendation({feasible:!0,best:candidate}))})})}function autoPackRegionsForCurrentSettings(){const hw=calculateOptimalHardware();hw.feasible?(applyHardwareRecommendation(hw),renderLog(`Auto-pack applied optimal config: ${hw.best.processors} ${hw.settings.procLabel}(s), ${hw.best.totalXds} ${hw.settings.unitLabel}(s).`)):renderLog(`Auto-pack failed: ${hw.error||"no feasible configuration found at current settings."}`)}function factorPairs(n){const pairs=[];for(let i=1;i*i<=n;i+=1)if(n%i===0){const a=i,b=n/i;pairs.push([a,b]),a!==b&&pairs.push([b,a])}return pairs}function getWeakestNode(){return state.nodes.reduce((acc,node)=>{if(!acc)return node;const accPixels=acc.maxWidth*acc.maxHeight,nodePixels=node.maxWidth*node.maxHeight,accMpix=acc.maxMPix,nodeMpix=node.maxMPix;return nodePixels<accPixels||nodePixels===accPixels&&nodeMpix<accMpix?node:acc},null)}function getWeakestProcessor(){return state.processors.reduce((acc,processor)=>{if(!acc)return processor;const capAcc=resolveProcessorCapacity(acc,state.metadata).capacityMPix;return resolveProcessorCapacity(processor,state.metadata).capacityMPix<capAcc?processor:acc},null)}function tileToScreen(tileX,tileY){return{x:view.panX+tileX*view.zoom,y:view.panY+tileY*view.zoom}}function screenToTile(screenX,screenY){return{x:Math.floor((screenX-view.panX)/view.zoom),y:Math.floor((screenY-view.panY)/view.zoom)}}function screenToTileFromClient(clientX,clientY){const rect=els.canvas.getBoundingClientRect();return screenToTile(clientX-rect.left,clientY-rect.top)}function inWall(x,y){return x>=0&&y>=0&&x<state.wall.wallTilesX&&y<state.wall.wallTilesY}function clampRegion(region){region.w=clampInt(region.w,1,state.wall.wallTilesX),region.h=clampInt(region.h,1,state.wall.wallTilesY),region.x=clampInt(region.x,0,Math.max(0,state.wall.wallTilesX-region.w)),region.y=clampInt(region.y,0,Math.max(0,state.wall.wallTilesY-region.h))}function rectOverlap(a,b){return a.x<b.x+b.w&&a.x+a.w>b.x&&a.y<b.y+b.h&&a.y+a.h>b.y}function colorForRegion(region,index){const nodeIndex=Math.max(0,state.nodes.findIndex(n=>n.id===region.nodeId));return palette[(region.nodeId?nodeIndex:index)%palette.length]}function colorForNode(nodeId){const nodeIndex=Math.max(0,state.nodes.findIndex(n=>n.id===nodeId));return palette[nodeIndex%palette.length]}function cableColorForPort(baseHex,portIndex,alpha=.3){const hsl=hexToHsl(baseHex),hueShift=(portIndex-1)%10*7;return`hsla(${(hsl.h+hueShift)%360}, ${clampInt(Math.max(72,hsl.s),60,100)}%, ${clampInt(Math.max(30,hsl.l-14),28,46)}%, ${alpha})`}function cableFillColorForStep(baseHex,portIndex,stepIndex,stepCount,alpha=.62){const hsl=hexToHsl(baseHex),hueShift=(portIndex-1)%10*7;return`hsla(${(hsl.h+hueShift)%360}, ${clampInt(Math.max(72,hsl.s),60,100)}%, ${lightnessForStep(clampInt(Math.max(30,hsl.l-14),28,46),stepIndex,stepCount)}%, ${alpha})`}function cableArrowColorForStep(baseHex,portIndex,stepIndex,stepCount,alpha=.9){return cableFillColorForStep(baseHex,portIndex,Math.max(0,stepCount-1-stepIndex),stepCount,alpha)}function lightnessForStep(baseLight,stepIndex,stepCount){const t=stepCount>1?stepIndex/(stepCount-1):0;return clampInt(baseLight-Math.round(16*t),12,58)}function hexToHsl(hex){const h=hex.replace("#",""),r=Number.parseInt(h.slice(0,2),16)/255,g=Number.parseInt(h.slice(2,4),16)/255,b=Number.parseInt(h.slice(4,6),16)/255,max=Math.max(r,g,b),min=Math.min(r,g,b),l=(max+min)/2,d=max-min;let hue=0,s=0;if(0!==d){switch(s=d/(1-Math.abs(2*l-1)),max){case r:hue=(g-b)/d%6;break;case g:hue=(b-r)/d+2;break;default:hue=(r-g)/d+4}hue=Math.round(60*hue),hue<0&&(hue+=360)}return{h:hue,s:Math.round(100*s),l:Math.round(100*l)}}function hexToRgba(hex,alpha){const h=hex.replace("#","");return`rgba(${Number.parseInt(h.slice(0,2),16)}, ${Number.parseInt(h.slice(2,4),16)}, ${Number.parseInt(h.slice(4,6),16)}, ${alpha})`}function selectField(label,path,options,current,title=""){const opts=options.map(v=>`<option value="${escapeAttr(v)}" ${v===current?"selected":""}>${escapeHtml(v)}</option>`).join("");return`<label class="field" title="${escapeAttr(title)}">${escapeHtml(label)}<select data-edit="${path}">${opts}</select></label>`}function slug(value){return String(value||"config").toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"")||"config"}function bad(message){return{level:"bad",message:message}}function warn(message){return{level:"warn",message:message}}function good(message){return{level:"good",message:message}}function uid(prefix){return`${prefix}-${Math.random().toString(36).slice(2,9)}`}function clampInt(v,min,max){const n=Number.parseInt(v,10);return Number.isFinite(n)?Math.max(min,Math.min(max,n)):min}function clampFloat(v,min,max){const n=Number.parseFloat(v);return Number.isFinite(n)?Math.max(min,Math.min(max,n)):min}function ensureArray(v){return Array.isArray(v)?v:[]}function loadHistoryStore(){if(historyStore.loaded)updateHistoryBadge();else{historyStore.loaded=!0;try{const raw=localStorage.getItem(HISTORY_STORAGE_KEY);if(!raw)return historyStore.entries=[],void updateHistoryBadge();const parsed=JSON.parse(raw);historyStore.entries=ensureArray(parsed).filter(entry=>entry&&"object"==typeof entry&&entry.id&&entry.snapshot).slice(-30)}catch{historyStore.entries=[]}updateHistoryBadge()}}function persistHistoryStore(){try{localStorage.setItem(HISTORY_STORAGE_KEY,JSON.stringify(historyStore.entries))}catch{}updateHistoryBadge()}function getHistoryEntriesNewestFirst(){return[...historyStore.entries].reverse()}function updateHistoryBadge(){els.historyBadgeCount&&(els.historyBadgeCount.textContent=String(historyStore.entries.length)),els.historyBtn&&(els.historyBtn.title=`Show history (${historyStore.entries.length} saved snapshot${1===historyStore.entries.length?"":"s"})`)}function shouldCaptureHistoryMessage(message){const msg=String(message||"").trim();return!!msg&&(!HISTORY_IGNORED_MESSAGES.has(msg)&&!msg.startsWith("Interaction complete"))}function pushHistoryEntry(message){if(!shouldCaptureHistoryMessage(message))return;loadHistoryStore();const snapshot=createProjectSnapshot(),now=Date.now(),last=historyStore.entries[historyStore.entries.length-1];last&&last.message===message&&now-(last.createdAt||0)<1200||(historyStore.entries.push({id:uid("hist"),createdAt:now,message:String(message),wallTilesX:snapshot.wall.wallTilesX,wallTilesY:snapshot.wall.wallTilesY,nodes:snapshot.nodes.length,processors:snapshot.processors.length,regions:snapshot.regions.length,snapshot:snapshot}),historyStore.entries.length>30&&(historyStore.entries=historyStore.entries.slice(historyStore.entries.length-30)),persistHistoryStore())}function createProjectSnapshot(){return JSON.parse(JSON.stringify({metadata:state.metadata,wall:state.wall,nodes:state.nodes,processors:state.processors,regions:state.regions,activeTab:state.activeTab}))}function restoreHistoryEntry(entryId){loadHistoryStore();const entry=historyStore.entries.find(item=>item.id===entryId);entry?.snapshot&&(applySnapshot(entry.snapshot),showToast(`Restored snapshot: ${entry.message}`))}function applySnapshot(snapshot){state.metadata={...state.metadata,...snapshot.metadata||{}},state.wall={...state.wall,...snapshot.wall||{}},sanitizeWall(),state.nodes=ensureArray(snapshot.nodes).map((n,i)=>({id:n.id||uid("node"),name:n.name||`RN-${i+1}`,maxWidth:clampInt(n.maxWidth,1,3e4),maxHeight:clampInt(n.maxHeight,1,3e4),maxMPix:clampFloat(n.maxMPix,.1,1e3),outputs:clampInt(n.outputs,1,16),backupNodeId:n.backupNodeId||""})),state.nodes.length||state.nodes.push({id:uid("node"),name:"RN-01",maxWidth:7680,maxHeight:4320,maxMPix:14,outputs:2,backupNodeId:""}),state.processors=ensureArray(snapshot.processors).map((p,i)=>{const processor={id:p.id||uid("proc"),name:p.name||`PROC-${i+1}`,type:p.type||"Custom Processor",profile:p.profile||state.metadata.processorProfile||"Brompton SX40",capacityMode:p.capacityMode||"custom",latencyMode:p.latencyMode||"From Project",customMPix:clampFloat(p.customMPix??p.maxMPix??9,.1,1e3)};return sanitizeProcessor(processor),processor}),state.processors.length||state.processors.push(defaultProcessor("PROC-A")),state.regions=ensureArray(snapshot.regions).map((r,i)=>({id:r.id||uid("reg"),name:r.name||`Region ${i+1}`,x:clampInt(r.x,0,1e4),y:clampInt(r.y,0,1e4),w:clampInt(r.w,1,1e4),h:clampInt(r.h,1,1e4),nodeId:r.nodeId||"",processorId:r.processorId||"",role:r.role||"Background"})),state.regions.length||state.regions.push({id:uid("reg"),name:"Main",x:0,y:0,w:12,h:8,nodeId:state.nodes[0].id,processorId:state.processors[0].id,role:"Frustum"}),state.activeTab=snapshot.activeTab||state.activeTab||"selection",syncProcessorProfiles(),state.selection={type:"region",id:state.regions[0]?.id||""},fitView(),renderAll("History snapshot restored")}function deleteHistoryEntry(entryId){loadHistoryStore(),historyStore.entries=historyStore.entries.filter(entry=>entry.id!==entryId),persistHistoryStore()}function clearHistoryEntries(){loadHistoryStore(),historyStore.entries=[],persistHistoryStore()}function formatHistoryTimestamp(ts){const date=new Date(ts);return Number.isNaN(date.getTime())?"Unknown time":date.toLocaleString()}function showPresetsModal(){loadPresetsStore();const existing=document.getElementById("presetsModal");existing&&existing.remove();const entries=getPresetEntriesNewestFirst(),modal=document.createElement("div");modal.id="presetsModal",modal.className="po-modal-backdrop",modal.innerHTML=`\n    <div class="po-modal presets-modal">\n      <div class="po-header">\n        <h2>Presets</h2>\n        <span class="po-sub">${entries.length} saved preset(s)</span>\n        <button class="tool-btn po-close" title="Close">✕</button>\n      </div>\n      <div class="po-body history-body">\n        <div class="history-toolbar">\n          <button class="tool-btn tiny" data-preset-action="save-current">Save Current</button>\n          <button class="tool-btn tiny" data-preset-action="export-csv" ${entries.length?"":"disabled"}>Export CSV</button>\n          <button class="tool-btn tiny" data-preset-action="clear" ${entries.length?"":"disabled"}>Clear Presets</button>\n        </div>\n        ${entries.length?`\n          <div class="history-list">\n            ${entries.map(entry=>`\n              <div class="history-item">\n                <div class="history-item-main">\n                  <div class="history-item-title">${escapeHtml(entry.name||"Preset")}</div>\n                  <div class="history-item-meta">${renderHistoryEntryMeta(entry)}</div>\n                </div>\n                <div class="history-item-actions">\n                  <button class="tool-btn tiny" data-preset-action="restore" data-preset-id="${entry.id}">Load</button>\n                  <button class="tool-btn tiny danger" data-preset-action="delete" data-preset-id="${entry.id}">Delete</button>\n                </div>\n              </div>`).join("")}\n          </div>`:'<div class="history-empty">No presets yet. Save your preferred configurations here for quick access.</div>'}\n      </div>\n    </div>`,document.body.appendChild(modal),modal.querySelector(".po-close")?.addEventListener("click",()=>modal.remove()),modal.addEventListener("click",e=>{if(e.target===modal)return void modal.remove();const btn=e.target.closest("[data-preset-action]");if(!btn)return;const action=btn.dataset.presetAction,id=btn.dataset.presetId;if("save-current"===action){const def=defaultPresetName(),name=window.prompt("Preset name:",def);return null!==name&&saveCurrentPreset(name),modal.remove(),void showPresetsModal()}return"restore"===action&&id?(restorePresetEntry(id),void modal.remove()):"delete"===action&&id?(deletePresetEntry(id),modal.remove(),void showPresetsModal()):void("export-csv"!==action?"clear"===action&&(clearPresetEntries(),modal.remove(),showPresetsModal()):exportEntriesCsv(entries,"presets"))})}function loadPresetsStore(){if(presetsStore.loaded)updatePresetsBadge();else{presetsStore.loaded=!0;try{const raw=localStorage.getItem(PRESETS_STORAGE_KEY);if(!raw)return presetsStore.entries=[],void updatePresetsBadge();const parsed=JSON.parse(raw);presetsStore.entries=ensureArray(parsed).filter(entry=>entry&&"object"==typeof entry&&entry.id&&entry.snapshot).slice(-30)}catch{presetsStore.entries=[]}updatePresetsBadge()}}function persistPresetsStore(){try{localStorage.setItem(PRESETS_STORAGE_KEY,JSON.stringify(presetsStore.entries))}catch{}updatePresetsBadge()}function updatePresetsBadge(){els.presetsBadgeCount&&(els.presetsBadgeCount.textContent=String(presetsStore.entries.length)),els.presetsBtn&&(els.presetsBtn.title=`Show presets (${presetsStore.entries.length} saved preset${1===presetsStore.entries.length?"":"s"})`)}function getPresetEntriesNewestFirst(){return[...presetsStore.entries].reverse()}function defaultPresetName(){return`${state.metadata.projectName||"LED Wall"} • ${(new Date).toLocaleString()}`}function saveCurrentPreset(nameRaw){loadPresetsStore();const name=String(nameRaw||"").trim();if(!name)return;const snapshot=createProjectSnapshot();presetsStore.entries.push({id:uid("preset"),name:name,createdAt:Date.now(),wallTilesX:snapshot.wall.wallTilesX,wallTilesY:snapshot.wall.wallTilesY,nodes:snapshot.nodes.length,processors:snapshot.processors.length,regions:snapshot.regions.length,snapshot:snapshot}),presetsStore.entries.length>30&&(presetsStore.entries=presetsStore.entries.slice(presetsStore.entries.length-30)),persistPresetsStore(),showToast(`Preset saved: ${name}`)}function restorePresetEntry(entryId){loadPresetsStore();const entry=presetsStore.entries.find(item=>item.id===entryId);entry?.snapshot&&(applySnapshot(entry.snapshot),showToast(`Preset loaded: ${entry.name}`))}function deletePresetEntry(entryId){loadPresetsStore(),presetsStore.entries=presetsStore.entries.filter(entry=>entry.id!==entryId),persistPresetsStore()}function clearPresetEntries(){loadPresetsStore(),presetsStore.entries=[],persistPresetsStore()}function computeSnapshotUnitCounts(snapshot){const profile=snapshot?.metadata?.processorProfile,reg=PROCESSOR_REGISTRY[profile];if(!reg?.portLookup)return null;const fps=reg.nearestFps?reg.nearestFps(snapshot.metadata.frameRate||"60"):parseInt(snapshot.metadata.frameRate||"60"),bpc=Number(snapshot.metadata.colorDepth)||10,latencyMode=snapshot.metadata.latencyMode||"Standard",portPixels=reg.portLookup(fps,bpc,latencyMode);if(!portPixels||portPixels<=0)return null;const tilePixels=Math.max(1,Number(snapshot.wall?.tilePxW)||1)*Math.max(1,Number(snapshot.wall?.tilePxH)||1),processors=ensureArray(snapshot.processors),regions=ensureArray(snapshot.regions);let totalUnits=0,totalParentUnits=0;return processors.forEach(proc=>{const totalPixels=regions.filter(r=>r.processorId===proc.id).reduce((sum,r)=>sum+r.w*r.h*tilePixels,0),ports=Math.max(1,Math.ceil(totalPixels/portPixels)),units=Math.ceil(ports/Math.max(1,reg.portsPerUnit));totalUnits+=units,reg.parentUnit&&(totalParentUnits+=Math.ceil(units/reg.parentUnit.unitsPerParent))}),reg.parentUnit&&(totalParentUnits=Math.max(processors.length,totalParentUnits),totalUnits=Math.max(totalParentUnits,totalUnits)),{unitLabel:reg.portUnitLabel||"XD",totalUnits:totalUnits,parentUnit:reg.parentUnit||null,totalParentUnits:reg.parentUnit?totalParentUnits:0}}function historyEntryDetails(entry){const snapshot=entry?.snapshot||{},wall=snapshot.wall||{},metadata=snapshot.metadata||{},processors=ensureArray(snapshot.processors),wallTilesX=Math.max(0,Number(wall.wallTilesX)||0),wallTilesY=Math.max(0,Number(wall.wallTilesY)||0),wallPxW=wallTilesX*Math.max(0,Number(wall.tilePxW)||0),wallPxH=wallTilesY*Math.max(0,Number(wall.tilePxH)||0),wallMetersW=wallTilesX*Math.max(0,Number(wall.tileMmW)||0)/1e3,wallMetersH=wallTilesY*Math.max(0,Number(wall.tileMmH)||0)/1e3,processorNames=processors.map((p,i)=>p?.name||`PROC-${i+1}`).join(", ")||"None",processorProfile=metadata.processorProfile||"-";return{wallPxW:wallPxW,wallPxH:wallPxH,wallMetersW:wallMetersW,wallMetersH:wallMetersH,wallTilesX:wallTilesX,wallTilesY:wallTilesY,fps:metadata.frameRate||"-",depth:metadata.colorDepth||"-",mode:"ULL"===metadata.latencyMode?"ULL":"Standard",processorProfile:processorProfile,processorNames:processorNames,unitCounts:computeSnapshotUnitCounts(snapshot)}}function renderHistoryEntryMeta(entry){const details=historyEntryDetails(entry),uc=details.unitCounts;let unitStr="-";return uc&&(unitStr=`${uc.totalUnits} ${uc.unitLabel}`,uc.parentUnit&&(unitStr+=` · ${uc.totalParentUnits} ${uc.parentUnit.label}`)),`\n    <div class="history-meta-grid">\n      <div class="history-meta-cell"><span class="history-meta-key">Time</span><span class="history-meta-val">${escapeHtml(formatHistoryTimestamp(entry.createdAt))}</span></div>\n      <div class="history-meta-cell"><span class="history-meta-key">Size (px)</span><span class="history-meta-val">${details.wallPxW}x${details.wallPxH}</span></div>\n      <div class="history-meta-cell"><span class="history-meta-key">Size (m)</span><span class="history-meta-val">${details.wallMetersW.toFixed(2)}m x ${details.wallMetersH.toFixed(2)}m</span></div>\n      <div class="history-meta-cell"><span class="history-meta-key">Video</span><span class="history-meta-val">${escapeHtml(`${details.fps} fps • ${details.depth} • ${details.mode}`)}</span></div>\n      <div class="history-meta-cell"><span class="history-meta-key">Processor Type</span><span class="history-meta-val">${escapeHtml(details.processorProfile)}</span></div>\n      <div class="history-meta-cell"><span class="history-meta-key">Processors</span><span class="history-meta-val">${escapeHtml(details.processorNames)}</span></div>\n      <div class="history-meta-cell"><span class="history-meta-key">Tiles</span><span class="history-meta-val">${details.wallTilesX}x${details.wallTilesY}</span></div>\n      <div class="history-meta-cell"><span class="history-meta-key">Dist. Units</span><span class="history-meta-val">${escapeHtml(unitStr)}</span></div>\n      <div class="history-meta-cell"><span class="history-meta-key">Counts</span><span class="history-meta-val">${entry.nodes} node(s), ${entry.processors} proc(s), ${entry.regions} region(s)</span></div>\n    </div>`}function exportEntriesCsv(entries,kind){if(!entries.length)return;const csv=[["kind","name","timestamp","size_px","size_m","fps","depth","latency_mode","processor_type","processor_names","tiles","nodes","processors","regions"],...entries.map(entry=>{const details=historyEntryDetails(entry);return[kind,entry.name||entry.message||"Snapshot",formatHistoryTimestamp(entry.createdAt),`${details.wallPxW}x${details.wallPxH}`,`${details.wallMetersW.toFixed(2)}x${details.wallMetersH.toFixed(2)}`,details.fps,details.depth,details.mode,details.processorProfile,details.processorNames,`${details.wallTilesX}x${details.wallTilesY}`,entry.nodes,entry.processors,entry.regions]})].map(row=>row.map(csvEscape).join(",")).join("\n"),blob=new Blob([csv],{type:"text/csv;charset=utf-8"}),url=URL.createObjectURL(blob),a=document.createElement("a");a.href=url,a.download=`${slug(state.metadata.projectName||"led-wall")}-${kind}-${Date.now()}.csv`,document.body.appendChild(a),a.click(),a.remove(),URL.revokeObjectURL(url),showToast(("history"===kind?"History":"Presets")+" CSV exported")}function csvEscape(value){const escaped=String(value??"").replaceAll('"','""');return/[",\n]/.test(escaped)?`"${escaped}"`:escaped}async function copyTextToClipboard(text){try{if(navigator?.clipboard?.writeText)return await navigator.clipboard.writeText(text),!0}catch{}try{const ta=document.createElement("textarea");ta.value=text,ta.setAttribute("readonly",""),ta.style.position="fixed",ta.style.left="-9999px",document.body.appendChild(ta),ta.select(),ta.setSelectionRange(0,ta.value.length);const ok=document.execCommand("copy");return ta.remove(),ok}catch{return!1}}function formatInt(n){return new Intl.NumberFormat("en-US").format(n)}function buildCablePlan({processorLoads:processorLoads,wall:wall}){const tilePixels=Math.max(1,wall.tilePxW*wall.tilePxH),processorPlans=[],warnings=[];return Object.values(processorLoads).forEach(load=>{const regions=[...load.regions].sort((a,b)=>a.y-b.y||a.x-b.x),perPortPixels=Math.max(1,load.xdPortCapacityPixels),panelCapByPixels=Math.max(1,Math.floor(perPortPixels/tilePixels)),panelsPerPortCap=Math.max(1,Math.min(500,panelCapByPixels)),ppu=load.portsPerUnit||10,usedPorts=Math.max(0,load.xdPortsRequired),xdCount=Math.max(0,load.xdsRequired),portSlots=Math.max(0,xdCount*ppu),ports=Array.from({length:portSlots},(_,i)=>({slot:i,xdIndex:Math.floor(i/ppu)+1,index:i%ppu+1,capacityPixels:perPortPixels,capacityPanels:panelsPerPortCap,usedPixels:0,usedPanels:0,segments:[]}));let portPtr=0;const nextAvailablePort=()=>{for(;portPtr<Math.min(usedPorts,ports.length);){const p=ports[portPtr],freePanels=p.capacityPanels-p.usedPanels,freePixelsTiles=Math.floor((p.capacityPixels-p.usedPixels)/tilePixels);if(Math.min(freePanels,freePixelsTiles)>0)return p;portPtr+=1}return null},pattern=state.metadata.wiringPattern||"linear",isColumnMajor="N"===pattern||"invN"===pattern,acquirePort=(regionName,needed)=>{let port=nextAvailablePort();if(!port)return warnings.push(`${load.processor.name}: insufficient planned ports while mapping "${regionName}".`),null;let freePanels=Math.max(0,port.capacityPanels-port.usedPanels),freePixelsTiles=Math.max(0,Math.floor((port.capacityPixels-port.usedPixels)/tilePixels)),canTake=Math.max(0,Math.min(needed,freePanels,freePixelsTiles));if(canTake<=0){if(portPtr+=1,port=nextAvailablePort(),!port)return warnings.push(`${load.processor.name}: insufficient planned ports while mapping "${regionName}".`),null;if(freePanels=Math.max(0,port.capacityPanels-port.usedPanels),freePixelsTiles=Math.max(0,Math.floor((port.capacityPixels-port.usedPixels)/tilePixels)),canTake=Math.max(0,Math.min(needed,freePanels,freePixelsTiles)),canTake<=0)return warnings.push(`${load.processor.name}: zero-capacity port slice encountered while mapping "${regionName}".`),null}return{port:port,canTake:canTake}};regions.forEach(region=>{if(isColumnMajor)for(let colIdx=0;colIdx<region.w;colIdx+=1){const x=region.x+colIdx,reversed="N"===pattern&&colIdx%2==1||"invN"===pattern&&colIdx%2==0;let remainingCol=region.h,yCursor=reversed?region.y+region.h-1:region.y;for(;remainingCol>0;){const result=acquirePort(region.name,remainingCol);if(!result)return;const{port:port,canTake:canTake}=result,px=canTake*tilePixels;port.usedPanels+=canTake,port.usedPixels+=px;const segY=reversed?yCursor-canTake+1:yCursor;port.segments.push({regionId:region.id,regionName:region.name,x:x,y:segY,w:1,h:canTake,vertical:!0,reversed:reversed,panels:canTake,pixels:px}),yCursor+=reversed?-canTake:canTake,remainingCol-=canTake,(port.usedPanels>=port.capacityPanels||port.capacityPixels-port.usedPixels<tilePixels)&&(portPtr+=1)}}else for(let y=region.y;y<region.y+region.h;y+=1){const rowIdx=y-region.y,reversed="S"===pattern&&rowIdx%2==1||"invS"===pattern&&rowIdx%2==0;let remainingRow=region.w,xCursor=reversed?region.x+region.w-1:region.x;for(;remainingRow>0;){const result=acquirePort(region.name,remainingRow);if(!result)return;const{port:port,canTake:canTake}=result,px=canTake*tilePixels;port.usedPanels+=canTake,port.usedPixels+=px;const segX=reversed?xCursor-canTake+1:xCursor;port.segments.push({regionId:region.id,regionName:region.name,x:segX,y:y,w:canTake,h:1,reversed:reversed,panels:canTake,pixels:px}),xCursor+=reversed?-canTake:canTake,remainingRow-=canTake,(port.usedPanels>=port.capacityPanels||port.capacityPixels-port.usedPixels<tilePixels)&&(portPtr+=1)}}});const xds=Array.from({length:xdCount},(_,xdIdx)=>({index:xdIdx+1,ports:ports.slice(xdIdx*ppu,(xdIdx+1)*ppu)}));processorPlans.push({processorId:load.processor.id,processorName:load.processor.name,portCapacityPixels:perPortPixels,panelCapacityPerPort:panelsPerPortCap,usedPorts:usedPorts,portUnitLabel:load.portUnitLabel||"XD",portUnitPrefix:PROCESSOR_REGISTRY[load.processor.profile]?.portUnitPrefix||"X",xds:xds})}),{processorPlans:processorPlans,warnings:warnings}}function formatCablePlan(cablePlan){const lines=[];return lines.push("--- Cable Plan (Processor -> Port Unit -> Port -> Region) ---"),cablePlan.processorPlans.forEach(plan=>{const unitLabel=plan.portUnitLabel||"XD";lines.push(`${plan.processorName}: port cap ${formatInt(plan.portCapacityPixels)} px, panel cap ${plan.panelCapacityPerPort}/port, used ports ${plan.usedPorts}`),plan.xds.forEach(xd=>{const xdRegionPanels={};xd.ports.forEach(port=>{port.segments.forEach(seg=>{xdRegionPanels[seg.regionName]=(xdRegionPanels[seg.regionName]||0)+seg.panels})});const xdPanelTotal=Object.values(xdRegionPanels).reduce((acc,v)=>acc+v,0),xdAssign=Object.entries(xdRegionPanels).map(([name,count])=>`${name}: ${count} panels`).join(", ");lines.push(`  ${unitLabel}-${xd.index} Assignment: ${xdPanelTotal} panels${xdAssign?` | ${xdAssign}`:" | none"}`),lines.push(`  ${unitLabel}-${xd.index}`),xd.ports.forEach(port=>{const utilPx=port.capacityPixels>0?port.usedPixels/port.capacityPixels*100:0,utilPanels=port.capacityPanels>0?port.usedPanels/port.capacityPanels*100:0,loadText=`${formatInt(port.usedPixels)} px (${utilPx.toFixed(1)}%), ${port.usedPanels} panels (${utilPanels.toFixed(1)}%)`;lines.push(`    P${String(port.index).padStart(2,"0")}: ${loadText}`);const portPath=buildPortTilePath(port);if(portPath.length){const baseHsl=hexToHsl(portPath[0].base),baseLight=clampInt(Math.max(30,baseHsl.l-14),28,46),highestLight=lightnessForStep(baseLight,0,portPath.length),lowestLight=lightnessForStep(baseLight,Math.max(0,portPath.length-1),portPath.length);lines.push(`      lightness steps: highest=${highestLight}% (step 1), lowest=${lowestLight}% (step ${portPath.length})`)}const portColorMap=new Map;if(port.segments.forEach(seg=>{const region=state.regions.find(r=>r.id===seg.regionId);if(!region)return;const regionIndex=Math.max(0,state.regions.findIndex(r=>r.id===region.id)),color=cableColorForPort(colorForRegion(region,regionIndex),port.index,1);portColorMap.has(seg.regionName)||portColorMap.set(seg.regionName,color)}),portColorMap.size){const colorText=Array.from(portColorMap.entries()).map(([name,color])=>`${name}=${color}`).join(", ");lines.push(`      color: ${colorText}`)}port.segments.length?port.segments.forEach(seg=>{lines.push(`      - ${seg.regionName} @ (${seg.x},${seg.y}) ${seg.w}x${seg.h} tiles`)}):lines.push("      - unused")})})}),cablePlan.warnings.length&&(lines.push(""),lines.push("Warnings:"),cablePlan.warnings.forEach(w=>lines.push(`- ${w}`))),lines}function escapeHtml(value){return String(value).replaceAll("&","&amp;").replaceAll("<","&lt;").replaceAll(">","&gt;").replaceAll('"',"&quot;").replaceAll("'","&#39;")}function escapeAttr(value){return escapeHtml(value).replaceAll("`","&#96;")}wire(),bootstrap(),showDisclaimer(),initTheme(),fitView(),renderAll("Ready");