import{st}from"./state.js";import{generateConfigString}from"./ui.js";export function applyFixedPreview(){const aw=Math.max(1,parseInt(st.controls.aspectW.value,10)||16),ah=Math.max(1,parseInt(st.controls.aspectH.value,10)||9);document.documentElement.style.setProperty("--aspect-w",`${aw}`),document.documentElement.style.setProperty("--aspect-h",`${ah}`);const r=st.holder.getBoundingClientRect();st.canvas.width=Math.max(1,Math.round(r.width*st.dpr)),st.canvas.height=Math.max(1,Math.round(r.height*st.dpr)),st.ctx.setTransform(1,0,0,1,0,0),st.ctx.clearRect(0,0,st.canvas.width,st.canvas.height)}export function getExportSize(){const w=Math.max(320,parseInt(st.controls.outputW.value,10)||1920),aw=Math.max(1,parseInt(st.controls.aspectW.value,10)||16),ah=Math.max(1,parseInt(st.controls.aspectH.value,10)||9),h=Math.round(w*(ah/aw));return{w:w,h:h,name:`${w}x${h}`}}export function computeCellGeometry(w,h,rows,cols){const cell=Math.max(1,parseInt(st.controls.cellSize.value,10)||50),gridW=cols*cell,gridH=rows*cell;return{w:w,h:h,cell:cell,gridW:gridW,gridH:gridH,ox:(w-gridW)/2,oy:(h-gridH)/2}}export function buildGridGeometry(startX,startY,rows,cols,tagPx,spacePx){const round=Math.round,colLefts=new Array(cols),colRights=new Array(cols),rowTops=new Array(rows),rowBottoms=new Array(rows);for(let c=0;c<cols;c++){const L=round(startX+c*(tagPx+spacePx)),R=round(L+tagPx);colLefts[c]=L,colRights[c]=R}for(let r=0;r<rows;r++){const T=round(startY+r*(tagPx+spacePx)),B=round(T+tagPx);rowTops[r]=T,rowBottoms[r]=B}return{colLefts:colLefts,colRights:colRights,rowTops:rowTops,rowBottoms:rowBottoms}}export function clearCanvas(ctx,w,h){ctx.fillStyle="#fff",ctx.fillRect(0,0,w,h)}export function drawFooterText(ctx,w,h){const configString=generateConfigString(),scale=w/1920,fontSize=Math.max(12,16*scale),padding=Math.max(15,Math.round(1.2*fontSize));ctx.font=`${fontSize}px "Roboto Mono"`,ctx.fillStyle="#888",ctx.textAlign="left",ctx.fillText("icvfxtools.com",padding,h-padding),ctx.textAlign="right",ctx.fillText(configString,w-padding,h-padding)}