import{arucoReady,drawArucoMarker}from"../aruco.js";import{computeCellGeometry}from"../geometry.js";export function drawCharuco(ctx,W,H,squaresY,squaresX,controls,bitDBAvailable){if(!arucoReady()&&!bitDBAvailable)return ctx.fillStyle="#fff",ctx.fillRect(0,0,W,H),ctx.fillStyle="#f00",ctx.textAlign="center",ctx.font="bold 16px Roboto Mono, monospace",void ctx.fillText("ArUco not available and JSON missing",W/2,H/2);const g=computeCellGeometry(W,H,squaresY,squaresX),cell=g.cell,topLeftBlack="black"===(controls.topLeft?.value||"black");for(let i=0;i<squaresY;i++)for(let j=0;j<squaresX;j++){const x=Math.round(g.ox+j*cell),y=Math.round(g.oy+i*cell),dark=(i+j)%2==0?topLeftBlack:!topLeftBlack;ctx.fillStyle=dark?"#000":"#fff",ctx.fillRect(x,y,cell,cell)}const placeOnBlack="black"===controls.charucoOn.value,ratio=Math.min(.85,Math.max(.5,parseFloat(controls.charucoRatio.value)||.7)),step=Math.max(1,parseInt(controls.charucoStep.value,10)||1);let id=Math.max(0,parseInt(controls.charucoStart.value,10)||0);const mSize=cell*ratio,margin=.5*(cell-mSize),dictName=controls.charucoDict.value,origin=controls.charucoOrigin.value,order=controls.charucoOrder.value,rowIdx=[...Array(squaresY).keys()],rowsSeq=(Array(squaresX).keys(),origin.includes("T")?rowIdx:rowIdx.slice().reverse()),ltrCols=[...Array(squaresX).keys()],rtlCols=ltrCols.slice().reverse();for(let rr=0;rr<rowsSeq.length;rr++){const i=rowsSeq[rr];let colsSeq;switch(order){case"row-major-ltr":default:colsSeq=ltrCols;break;case"row-major-rtl":colsSeq=rtlCols;break;case"snake-ltr-first":colsSeq=rr%2==0?ltrCols:rtlCols;break;case"snake-rtl-first":colsSeq=rr%2==0?rtlCols:ltrCols}for(const j of colsSeq){const dark=(i+j)%2==0?topLeftBlack:!topLeftBlack;if(!(placeOnBlack?dark:!dark))continue;const x=g.ox+j*cell+margin,y=g.oy+i*cell+margin;if(drawArucoMarker(ctx,x,y,mSize,id,dictName),id+=step,controls.charucoShowId.checked){const fontSize=Math.max(8,Math.min(16,.15*cell));ctx.font=`${fontSize}px "Roboto Mono"`,ctx.fillStyle=placeOnBlack?"#fff":"#000",ctx.textAlign="center",ctx.fillText(String(id-step),x+mSize/2,y+mSize+1.1*fontSize)}}}}