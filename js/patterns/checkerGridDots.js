import{computeCellGeometry}from"../geometry.js";export function drawChecker(ctx,W,H,rowsInner,colsInner,controls){const rows=rowsInner+1,cols=colsInner+1,g=computeCellGeometry(W,H,rows,cols),cell=g.cell,topLeftBlack="black"===(controls.topLeft?.value||"black");for(let i=0;i<rows;i++)for(let j=0;j<cols;j++){const x=Math.round(g.ox+j*cell),y=Math.round(g.oy+i*cell),dark=(i+j)%2==0?topLeftBlack:!topLeftBlack;ctx.fillStyle=dark?"#000":"#fff",ctx.fillRect(x,y,cell,cell)}}export function drawGridLines(ctx,W,H,rowsInner,colsInner,controls){const rows=rowsInner+1,cols=colsInner+1,g=computeCellGeometry(W,H,rows,cols),cell=g.cell;ctx.strokeStyle="#000",ctx.lineWidth=2,ctx.beginPath();const x_start=Math.round(g.ox),x_end=Math.round(g.ox+g.gridW),y_start=Math.round(g.oy),y_end=Math.round(g.oy+g.gridH);for(let j=1;j<cols;j++){const x=Math.round(g.ox+j*cell);ctx.moveTo(x,y_start),ctx.lineTo(x,y_end)}for(let i=1;i<rows;i++){const y=Math.round(g.oy+i*cell);ctx.moveTo(x_start,y),ctx.lineTo(x_end,y)}ctx.stroke()}export function drawDots(ctx,W,H,rowsInner,colsInner,controls){const g=computeCellGeometry(W,H,rowsInner+1,colsInner+1),cell=g.cell,radius=(parseFloat(controls.dotRadius.value)||.1)*cell;ctx.fillStyle="#000";for(let i=1;i<=rowsInner;i++)for(let j=1;j<=colsInner;j++)ctx.beginPath(),ctx.arc(Math.round(g.ox+j*cell),Math.round(g.oy+i*cell),radius,0,2*Math.PI),ctx.fill()}